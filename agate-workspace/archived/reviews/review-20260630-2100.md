# P2.1/P2.10 降级方案 v2 实施评审

> 评审对象：`f7cd6f0`（v2 实现）+ `a1dfcb1`（评审修复）
> 评审日期：2026-06-30
> 设计文档：docs/plans/agent-provenance-design-2026-06-30.md

---

## 评审发现

### Critical（3 项，全部已修复）

| 编号 | 问题 | 修复 |
|------|------|------|
| C1 | agent 字段循环无阶段守卫，阻塞所有现有任务 commit | 加 `if [ -f "$P6_FILE" ]` 守卫 |
| C2 | `grep -qF` 子字符串匹配导致审计 1c 假阴性（`ac1.png` 匹配 `mac1.png`） | 改为 `grep -qE "\([^)]*${ev_basename}\)"` 括号上下文精确匹配 |
| C3 | 审计 1c 搜索整个 P6 文件而非仅 PASS 行，FAIL 行/注释引用也可满足 | 改为先 `grep -E '^\s*- PASS\b'` 再搜索 |

### Important（7 项，全部已修复）

| 编号 | 问题 | 修复 |
|------|------|------|
| I1 | state-machine.md + WORKFLOW.md 缺 `exit 2`，与 dispatch-protocol.md 不一致 | 加 `或 exit 2` |
| I2 | `(PASS\|FAIL)` 缺词边界，`- FAILURE` 误匹配 | 加 `\b` |
| I3 | 证据引用提取取第一个括号组，多括号行误取 | 改取行末最后一个括号组 `grep -oE '\([^)]+\)$'` |
| I4 | `evidences/` 旧前缀未 strip，旧格式 P6 文件 1a 通过但 1c 失败 | 加 strip + 1c 匹配兼容子目录路径 |
| I5 | `.gitkeep` 被计为证据文件 | find 加 `-not -name '.*'` |
| I6 | CI 不重跑 check-p6-provenance.sh，`--no-verify` 绕过 provenance | 在 LIMITATIONS.md 明确记录为已知局限 |
| I7 | 设计文档代码块 BDD 计数正则与实现不一致 | 实现（Given only）正确，文档说明即可 |

### Minor（4 项，不阻塞）

| 编号 | 问题 | 处理 |
|------|------|------|
| M1 | `PASS_COUNT` 双赋值（tail -1 冗余） | 保留——防御性编码，与现有脚本风格一致 |
| M2 | `get_agent()` 不验证 agent 值是否合法 | agent 字段定位为协作规范，格式验证收益低 |
| M3 | CI git blame 在新文件上总触发 WARNING | 低噪音，首次 P6 commit 单 author 是正常的 |
| M4 | LIMITATIONS.md 未提 `--no-verify` 绕过 | I6 修复时已追加 |

---

## 实测结果

| 场景 | 预期 | 修复前 | 修复后 |
|------|------|--------|--------|
| 合规（2 PASS + 2 证据 + 子目录引用 + agent 字段） | exit 0 | exit 1 (1c 假阳性) | ✅ exit 0 |
| 空证据 | exit 1 | ✅ exit 1 | ✅ exit 1 |
| 挑验（P6 < P1 BDD） | exit 1 | ✅ exit 1 | ✅ exit 1 |
| 空 png 充数（未引用证据文件） | exit 1 | exit 0 (C2/C3) | ✅ exit 1 |
| dispatch-context 预判 | exit 1 | ✅ exit 1 | ✅ exit 1 |
| P1 BDD 非标准格式 | exit 2 | ✅ exit 2 | ✅ exit 2 |
| 缺 agent 字段 | exit 1 | ✅ exit 1 | ✅ exit 1 |
| risk=high 自审 | exit 2 | ✅ exit 2 | ✅ exit 2 |
| evidences/ 旧前缀引用 | exit 0 | exit 1 (I4) | ✅ exit 0 |
| .gitkeep 不算证据 | exit 1 | exit 0 (I5) | ✅ exit 1 |

---

## 结论

3 Critical + 7 Important 全部修复，实测通过。v2 降级方案实现可发布。
