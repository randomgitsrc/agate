---
phase: P6.5
task_id: TAG0021-structured-layer
type: review-progress
agent: judge
created: 2026-08-22
status: completed
---

# P6.5 judge 复核进度（TAG0021-structured-layer）

> 状态标记：[PROD_NOT_TOUCHED]（仅写本进度文件 + verdict；稳定版 ~/.agate 与主 checkout 未动）

## 复核范围与独立验证手段

- 白名单输入已读：P1-requirements.md（16 BDD 权威判据）、P2-design.md §3.7 + gate_commands、P6-evidence/ 全部 18 个证据文件、.state.yaml（p5_pass_commit=14aa44f）、gate-events.jsonl（P1→P6 事件链完整）、git log（P1 b50206f → P6 be20a21）
- 独立实跑（worktree 只读，scratch 仅用 dist/ basetemp）：check-yaml-schema / check-structure-consistency / count-tests / check-platform-assumptions / 定向 pytest 5 文件 33 用例 / 全量 pytest / check-gate P2 对账 / check-protocol-consistency --strict-errors-only
- 全量 pytest 复现：1198 passed / 2 failed（test_bdd_7 + test_bdd_25）/ 2 skipped——与 bdd-4-pytest-full.log 完全一致
- test_bdd_25 隔离复跑 1 passed（共享 basetemp 污染确证，非回归）
- test_bdd_7 涉及文件（test_check_routing.py / check-routing.py / conftest.py）git log 9c50243..HEAD 零改动 → 沙箱 git 上下文 CAPABILITY_GAP，环境固有
- 首轮 consistency exit 1 系我自身 pytest 污染 dist/ 所致；清空 dist/ 后重跑 exit 0（318 WARNING 无 ERROR），与 bdd-4-consistency.log 一致

## 逐组进展

### M0（BDD-1..5）— 已验
- BDD-1 check-yaml-schema exit 0（独立复跑）；rules/ 3 YAML + 3 schema 存在
- BDD-2 check-structure-consistency S1/S2 OK exit 0（独立复跑）；反例在 pytest（test_bdd_2_*_exit_1 全过）+ bdd-2-s1s2-neg.log
- BDD-3 S5/S6 OK exit 0（独立复跑）；反例 test_bdd_3_*_exit_1 全过 + bdd-3-s5s6-neg.log
- BDD-4 全量 pytest 复现 2 环境假象（见上）；count 1202 ≥ 749；consistency 清 dist 后 0 ERROR exit 0
- BDD-5 S3/S4 OK exit 0（独立复跑）；反例 test_bdd_5_*_exit_1 全过

### M1（BDD-6..7）— 已验
- BDD-6 实测 check-gate P2 对账：RECONCILE SUMMARY 0 mismatches + exit 2（原语义不变）；bdd-6-reconcile-warning.log 反例含 WARNING + 计数；reconcile_field/summary 调用点在 3 脚本中 grep 证实
- BDD-7 3 脚本（read-gate-commands/pruning/check-gate）× 3 类解析点（gate_commands/P1 裁剪/P2 四字段）grep 证实 + test_check_reconcile 7 passed

### M2（BDD-8..11）— 已验
- BDD-8 真实任务对账 0 mismatches（独立复跑）+ test_bdd_8_* 全过
- BDD-9 静态扫描 0 命中（独立 grep A/B 字面量全 0）+ test_bdd_9_* 全过
- BDD-10 三处阻断：脚本漂移 exit 1（test_bdd_10_script_drift_blocked 过 + s1s2-neg.log）；pre-commit-gate.py 2j.2 step（行 410-419）存在；CI workflow step（行 131-132）存在
- BDD-11 同 BDD-4 全量证据（bdd-4-pytest-full.log 标 BDD-4/11/14）

### M3（BDD-12..14）— 已验
- BDD-12 S3 正向 exit 0（独立复跑）+ 篡改反例 exit 1（test_bdd_12_tampered_yaml_detected 过 + bdd-12-s3-tamper.log）
- BDD-13 test_bdd_13_inject_renders_from_yaml + test_bdd_13_stable_isolation_not_polluted 过（稳定版不被 worktree 未发布 YAML 污染，AGATE_ROOT 双根假树验证）
- BDD-14 全量 + count 1202 + consistency 0 ERROR + structure 全 OK

### 跨里程碑（BDD-15..16）— 已验
- BDD-15 count 1202（独立复跑）≥ 749 基线；git log P3 1168→1202 单调
- BDD-16 check-platform-assumptions exit 0（独立复跑）

## 结论
16/16 全部独立复核通过，证据文件均存在非空。待写 verdict。

## 格式修复轮（增量，2026-08-22）

- 修复 1（任务主诉）：16 条结论行前缀 `- (PASS)` → `- PASS`（对齐 check-judge-verdict.py `_CONCLUSION_RE`，去掉括号）；BDD 编号/描述不变。
- 发现并修复 2（gate 复跑暴露的潜伏问题）：结论行证据引用 `(P6-evidence/bdd-*.log)` 与 frontmatter `verdict_evidence` 裸文件名清单（`bdd-*.log`）字符串不对称 → `_check_evidence` 6c 引用对称校验 exit 1。按 P3 固化口径与 judge.md「引用须在 verdict_evidence 清单内」规范，将结论行引用归一为裸文件名 `(bdd-*.log)`（同一证据文件，仅路径形态归一；`verdict_evidence` 必须保持裸名，前缀化会使文件存在性校验失败）。判定内容/编号/证据实体均未改动。
- 复跑：`cd /home/kity/oclab/agate/.worktrees/agate-TAG0021 && timeout 60 python3 /home/kity/oclab/agate/agate/scripts/check-gate.py P6.5 agate-workspace/tasks/TAG0021-structured-layer`
- 实际输出：`GATE JUDGE-VERDICT: 校验通过（status=passed, criteria 16/16），judge_verdict 事件已记账` / `GATE EVENTS: 账本审计通过（13 行，哈希链完整，ts 单调，judge 轮次×1）` / `GATE P6.5: judge 复核 + 账本审计通过`
- 退出码：0（GATE JUDGE-VERDICT 通过 + GATE P6.5 通过）
- 自检：`^- (PASS)` 命中 0；`^\s*- PASS BDD-` 命中 16。
- 状态标记：[PROD_NOT_TOUCHED]（本轮仅写 P6.5-judge-verdict.md + 本进度文件；稳定版 ~/.agate 与主 checkout /home/kity/oclab/agate 未动）