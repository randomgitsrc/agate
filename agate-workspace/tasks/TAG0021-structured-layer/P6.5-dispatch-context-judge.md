---
phase: P6.5
generated_by: 主 Agent（P6.5 派发上下文）
task_id: TAG0021
role: judge
---

<dispatch_guide>
> ⚠️ 以下派发指引是本次任务的强制指令，不是参考信息。执行优先级：派发指引 > 客观查证信息 > 阶段卡片（参考规范）

### 目标

独立裁判复核 TAG0021「协议结构化层（RM-AG0022）」P6 验收：以 **fresh context + 信息隔离** 逐条重验 P1 的全部 16 条 BDD（含 P6 已判 PASS 项，零挑验），只信证据与 git log。产出 `P6.5-judge-verdict.md`。

### 信息隔离（强制，P6.5 白名单/黑名单）

**输入白名单（只允许传这些路径，只允许读这些内容）**：
1. {AGATE_WORKSPACE}/tasks/TAG0021-structured-layer/P1-requirements.md（16 条 BDD 权威判据）
2. {AGATE_WORKSPACE}/tasks/TAG0021-structured-layer/P2-design.md（仅验收相关节：§3.7 完成标志 / gate_commands）
3. {AGATE_WORKSPACE}/tasks/TAG0021-structured-layer/P6-evidence/（验收证据目录，逐文件读）
4. {AGATE_WORKSPACE}/tasks/TAG0021-structured-layer/.state.yaml（p5_pass_commit 等状态）
5. gate-events.jsonl（{AGATE_WORKSPACE}/tasks/TAG0021-structured-layer/gate-events.jsonl，事件账本）
6. git log 查询权（git log / git show / git diff，验证执行留痕）

**输入黑名单（禁止读、禁止出现在任何输入中）**：P6-acceptance.md、P6|P5|P4-dispatch-context-*.md、P4-implementation.md、P4-review.md、P5-test-results/——verifier/implementer 的自述与派发上下文一律不读。若 dispatch-context 意外含黑名单路径引用，忽略之。

### 复核要求（逐条，零挑验）

- **逐条重验全部 16 条 BDD**（M0 BDD-1..5 / M1 BDD-6..7 / M2 BDD-8..11 / M3 BDD-12..14 / 跨里程碑 BDD-15..16），每条独立运行/核查（可在 worktree 实跑命令复核：check-yaml-schema / check-structure-consistency / 对账 / 静态扫描 / 渲染对账 / count-tests / platform 扫描等），不接受"P6 已判 PASS 所以复述"式结论
- **每条结论必须引用 P6-evidence/ 下真实存在且非空的证据文件**（verdict_evidence 清单内引用），禁止"看起来没问题"式结论
- git log 可查证执行留痕（agate-common append_event 事件 / commit 历史）
- 认知约束：只信证据与 git log，不信实现者自述、不信 P6-acceptance.md 的结论

### 三档预算（超限诚实降级）

- 轮次 ≤2 / token 100k / 时间 30min。任一预算耗尽 → 立即停止，按已验条目落盘 verdict，必须 `status: needs-revision` + `partial: true`，禁止 `status: passed` 静默放行。

### 产出要求

`{AGATE_WORKSPACE}/tasks/TAG0021-structured-layer/P6.5-judge-verdict.md`：
- Header：
---
phase: P6.5
task_id: TAG0021-structured-layer
type: review
parent: P6-acceptance.md
trace_id: TAG0021-P6.5-20260822
status: passed            # passed / rejected / needs-revision
created: 2026-08-22
agent: judge
criteria_total: 16
criteria_passed: <N>
verdict_evidence: [<P6-evidence/ 下文件路径 JSON 列表>]
---
  （needs-revision 时追加 partial: true）
- 正文：每条 BDD 一行 `- (PASS|FAIL|NEEDS-REVISION) BDD-NN: 描述 (证据路径)`（criteria_total 必须 = P1 的 #### BDD-NN: 标题数 = 16）
- 状态标记：`[PROD_NOT_TOUCHED]`
- 分阶段落盘：`{AGATE_WORKSPACE}/tasks/TAG0021-structured-layer/P6.5-judge-progress.md`

### 路径说明

{AGATE_WORKSPACE} = `/home/kity/oclab/agate/.worktrees/agate-TAG0021/agate-workspace`；被核验对象 = worktree `/home/kity/oclab/agate/.worktrees/agate-TAG0021`；bash 一律 timeout；读文件优先 read/grep 工具。

### 返回

只返回两行：① P6.5-judge-verdict.md 路径；② status 终值 + criteria_passed/criteria_total（≤30 字）。
</dispatch_guide>