---
phase: P6.5
generated_by: agate-inject-card.py + 主 Agent
task_id: TAG0026
role: judge
---

<dispatch_guide>
> ⚠️ 以下派发指引是本次任务的强制指令，不是参考信息。执行优先级：派发指引 > 客观查证信息 > 阶段卡片（参考规范）

### 目标

独立复核 TAG0026 的 P6 验收：逐条重验**所有 13 条 BDD**（含 P6 已判 PASS 项，零挑验），
只信证据与 git log，产出 `P6.5-judge-verdict.md`。

### 白名单输入（只能读这些，信息隔离）

- `agate-workspace/tasks/TAG0026-maintainability-gate/P1-requirements.md`（BDD 原文 §7）
- `agate-workspace/tasks/TAG0026-maintainability-gate/P2-design.md`（仅验收相关节：§3 契约）
- `agate-workspace/tasks/TAG0026-maintainability-gate/P6-evidence/`（全部证据文件）
- `agate-workspace/tasks/TAG0026-maintainability-gate/.state.yaml`
- `agate-workspace/tasks/TAG0026-maintainability-gate/gate-events.jsonl`
- git log / git show（只读查询权）

### 黑名单（禁止读，禁含于本 dispatch-context 之外的重读）

P6-acceptance.md、P6-dispatch-context-verifier.md、P5/P4/P3/P2/P1-dispatch-context-*.md
（P1-requirements 除外，见白名单）、P4-implementation.md、P4-review.md、P5-test-results/、
P6-progress.md、P5-progress.md、P4-progress.md——verifier/implementer 的自述与派发上下文
一律不读。

### 约束

1. **零挑验**：13 条 BDD 逐条重验。每条结论必须引用 P6-evidence/ 下真实存在且非空的
   证据文件（verdict_evidence 清单内），禁止"看起来没问题"式结论。
2. **重验方式**：对每条 BDD，读证据 log 核对其中命令与输出是否真正支撑该 BDD 的判定锚
   （P1 §7 每条写明的 PASS/FAIL 判定锚）；对可机械复核的证据（如 CLI exit code、gate 四态
   输出）可自行重放确认（tmp 仓库，禁 worktree git 写）。
3. **三档预算（超限诚实降级）**：轮次 ≤2 / token 100k / 时间 30min。任一耗尽 → 立即停止，
   按已验条目落盘 verdict，必须 status: needs-revision + partial: true，禁止 status: passed
   静默放行。
4. **verdict 产出格式**（硬约束）：
   - Header（agate-md-field-set 或与 --list 字段一致）：`status`（passed/rejected/
     needs-revision）+ `criteria_total: 13` + `criteria_passed: N` + `verdict_evidence`
     （JSON 列表，引用的证据文件相对 P6-evidence/ 路径）+（partial 可选 true）
   - `phase: P6.5` / `task_id: TAG0026` / `parent: P6-acceptance.md` /
     `trace_id: TAG0026-P65-20260830` / `created: 2026-08-30` / `agent: judge`
   - 正文：每条 BDD 一行 `- (PASS|FAIL|NEEDS-REVISION) BDD-NN: 描述 (证据路径)`
   - criteria_total 必须等于 P1 `#### BDD-NN:` 标题数（13）
5. **git log 查证**：可用 git log 确认执行留痕（P4 f7e7b9f 实现、P5 acf0cb2 验证、
   P6 7af1e72 验收）。
6. **PROD 隔离**：全程 tmp 仓库重验，禁 worktree git 写；返回报 [PROD_NOT_TOUCHED]。
7. 所有 bash `timeout 280` 上限；分阶段落盘 **P6.5-progress.md**（防中断丢失，每验一条
   BDD 追加一行）。

### 产出（路径硬约束）

/home/kity/oclab/agateon/.worktrees/agate-TAG0026/agate-workspace/tasks/TAG0026-maintainability-gate/P6.5-judge-verdict.md
</dispatch_guide>

<!-- AGATE_CARD_START -->
（当前阶段卡片由 agate-inject-card.py 自动注入，此处省略）
<!-- AGATE_CARD_END -->

<objective_info>
### A. 环境（主 Agent 已核，judge 可信）
- worktree 根 /home/kity/oclab/agateon/.worktrees/agate-TAG0026；HEAD=7af1e72（P6 commit）
- 关键 git 留痕：f7e7b9f（P4 实现 M1-M8）→ acf0cb2（P5 五命令全绿 1333 passed）→ 7af1e72（P6 验收 13/13）
- judge.enabled: true 已在 .state.yaml（P1 初始化）
- P6-evidence/ 现有 13 份 log（bdd-1..13.log），均 >1KB
- P6 gate 链已过：check-gate.py P6 exit 2 / check-p6-evidence exit 0 / check-p6-provenance exit 0
- 你的重验是独立新鲜上下文：不要试图读取任何黑名单文件——它们不在你的工作范围，读了即违规
</objective_info>

> 注：该文件禁止包含 PASS/FAIL 预判——否则被 `check-p6-provenance.py` 审计失败。
