---
phase: P6.5
generated_by: agate-inject-card.py + 主 Agent
task_id: TAG0024
role: judge
---

<dispatch_guide>
> ⚠️ 以下派发指引是本次任务的强制指令，不是参考信息。执行优先级：派发指引 > 客观查证信息 > 阶段卡片（参考规范）

### 目标
独立 judge 复核（fresh context，信息隔离）：逐条重验 P1-requirements.md 全部 30 条 BDD（含已判 PASS 项，零挑验），只信证据文件与 git log，不信任何执行/验收角色的自述结论。

### 约束（信息隔离，机械校验，违反即 gate 拦截）
- **白名单输入**（仅允许读取以下路径）：`P1-requirements.md`、`P2-design.md`（仅验收相关节，如 gate_commands/BDD 相关设计）、`P6-evidence/` 目录（全部原始证据文件）、`.state.yaml`、`gate-events.jsonl`；另授权查询 git log（`git log`/`git show`/`git diff`，不限于本任务分支）
- **黑名单禁读**（禁止读取，即使被提及也不得打开）：`P6-acceptance.md`（verifier 自述结论，防锚定）、`P4-dispatch-context-*.md`/`P5-dispatch-context-*.md`/`P6-dispatch-context-*.md`（执行/验收角色的派发上下文）、`P4-implementation.md`（及其批次子文件）、`P4-review.md`、`P5-test-results/`、`P6-batch-results-*.md`（三批次的中间汇总文件，同样是"自述"性质）
- **认知约束**：逐条重验所有 30 条 BDD（不得只抽查部分），每条结论必须引用 `P6-evidence/` 下真实存在且非空的证据文件（`verdict_evidence` 清单内），禁止"看起来没问题"式结论；可用 git log 查证代码改动确实落地
- **三档预算**：复核轮次 ≤2（本次为第 1 轮）/ token 100k（可被 `judge_token_budget` 覆盖，本任务未覆盖，用默认值）/ 时间 30min。任一预算耗尽须立即停止，按已验条目落盘 `status: needs-revision` + `partial: true`，不得 `status: passed` 静默放行

### 上游关联
（本节按信息隔离规则不注入任何执行/验收阶段的产出结论摘要——judge 必须自己从白名单路径独立得出结论）

### 输入文件（仅限白名单，不得访问列表外任何文件）
- {AGATE_WORKSPACE}/tasks/TAG0024-toolchain-md-field-set/P1-requirements.md
- {AGATE_WORKSPACE}/tasks/TAG0024-toolchain-md-field-set/P2-design.md
- {AGATE_WORKSPACE}/tasks/TAG0024-toolchain-md-field-set/P6-evidence/（目录，全部文件）
- {AGATE_WORKSPACE}/tasks/TAG0024-toolchain-md-field-set/.state.yaml
- {AGATE_WORKSPACE}/tasks/TAG0024-toolchain-md-field-set/gate-events.jsonl
</dispatch_guide>

<!-- AGATE_CARD_START -->
<!-- AGATE_CARD_END -->

<objective_info>
- 环境状态：P6 commit 为 `cb7799c`（HEAD），P6-evidence/ 下含 3 个批次目录（md-field-set-tool/check-gate-debt-fixes/phases-yaml-consistency），共 27 个证据文件，覆盖 BDD-1~30
</objective_info>
