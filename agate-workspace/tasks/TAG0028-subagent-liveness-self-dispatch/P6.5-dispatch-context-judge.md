---
phase: P6.5
generated_by: 主 Agent
task_id: TAG0028
role: judge
---

<dispatch_guide>
> ⚠️ 以下派发指引是本次任务的强制指令，不是参考信息。执行优先级：派发指引 > 客观查证信息 > 角色定义（judge.md）

### 目标

以 fresh context 逐条重验 P1 的 33 条 BDD（含 P6 已判 PASS 项，**零挑验**），产出 `P6.5-judge-verdict.md`。
只凭标准（P1 BDD + P2 验收设计相关节）与证据文件、git log 判案，不信任何实现者自述。

### 信息隔离（强制，白名单/黑名单）

**输入白名单（dispatch-context 只含这些路径，你只读这些）**：
1. `agate-workspace/tasks/TAG0028-subagent-liveness-self-dispatch/P1-requirements.md`（BDD 标准，逐条判案唯一依据）
2. `agate-workspace/tasks/TAG0028-subagent-liveness-self-dispatch/P2-design.md`（仅验收相关节，如 gate_commands/验收设计）
3. `agate-workspace/tasks/TAG0028-subagent-liveness-self-dispatch/P6-evidence/` 目录（证据文件，逐条引用）
4. `agate-workspace/tasks/TAG0028-subagent-liveness-self-dispatch/.state.yaml`（任务状态）
5. `agate-workspace/tasks/TAG0028-subagent-liveness-self-dispatch/gate-events.jsonl`（事件账本，交叉核对执行留痕）
6. 另授 **git log 查询权**（查证命令执行、commit 存在性、author）

**禁止输入（黑名单，禁读禁引用）**：
- `P6-acceptance.md`（verifier 自述——防锚定，不读）
- `P6-dispatch-context-*.md` / `P5-dispatch-context-*.md` / `P4-dispatch-context-*.md`（派发上下文）
- `P4-implementation.md` / `P4-review.md`（实现与评审产出）
- `P5-test-results/`（技术验证自述）
- 行首 `- PASS` / `- FAIL` 验收结论预判（禁止在评审/派发文件中写这类预判）

### 认知约束（四条强制，judge.md）

1. **逐条重验所有 BDD**：33 条 → verdict 必须给出 33 条独立结论（含 P6 已判 PASS 项）——挑验即违规
2. **只信证据与 git log，不信叙述**：每条结论依据只能是 P6-evidence/ 下证据文件内容 + git log 可查证记录
3. **每条结论必须引用证据路径**：格式 `- (PASS|FAIL|NEEDS-REVISION) BDD-NN: {描述} ({证据路径})`；
   引用须在 verdict_evidence 清单内且指向真实存在、非空的证据文件
4. **禁止"看起来没问题"式结论**：证据不足 → FAIL 或 NEEDS-REVISION，不猜不放过

### 三档预算（超限诚实降级）

| 维度 | 上限 | 超限行为 |
|------|------|---------|
| 复核轮次 | ≤2 轮 | 超限交人工，不自行再复核 |
| token | 100k | 停止 → partial: true |
| 时间 | 30 分钟 | 同上 |

预算耗尽 → 立即停止，按已验条目落盘，必须 `status: needs-revision` + `partial: true` + 说明未验完条目。

### 产出

`agate-workspace/tasks/TAG0028-subagent-liveness-self-dispatch/P6.5-judge-verdict.md`：

```markdown
---
status: passed            # passed | rejected | needs-revision
criteria_total: 33        # == P1 #### BDD-NN: 标题数
criteria_passed: 33       # status=passed 时须 == criteria_total
verdict_evidence: [...]   # 证据清单（每条结论引用 ⊆ 此清单，且逐条被引用）
partial: false
---

逐条结论（33 条，零挑验）：
- 每条格式：`- (PASS|FAIL|NEEDS-REVISION) BDD-NN: {描述} ({证据路径})`（行首 PASS/FAIL 仅 verdict 文件使用）
- 示例：PASS BDD-1: {描述} ({证据路径})（说明文字，非验收预判）
```

### 返回给主 Agent

`File: <路径>` + `Status: <passed|rejected|needs-revision>`

### 门槛（什么算完成）

- P6.5-judge-verdict.md 存在且非空，Header 字段机器可读（status/criteria_total=33/criteria_passed/verdict_evidence/partial）
- 33 条结论行 `- (PASS|FAIL|NEEDS-REVISION) BDD-NN:`，每条带真实存在的证据路径引用
- 你的 verdict 只是行为描述输入——放行由机械核对（check-judge-verdict.py + check-events.py）决定，你只产出 verdict 文件
</dispatch_guide>

<objective_info>
### A. BDD 标准（P1，33 条，逐条判案依据——读 P1-requirements.md 获取全文）
- Phase 1 解析层 BDD-1~7 / Phase 2 检测引擎 BDD-8~24 / Phase 3 心跳 BDD-25~28 / Phase 4 再派发 BDD-29~33

### B. 证据目录（P6-evidence/，36 文件）
- bdd-01..33.log（每条 BDD 一个 pytest -k 实跑 log，含 EXIT_CODE: 0 尾行）+ bdd-07-sanitize.log +
  bdd-26-cli.log + bdd-33-git.log

### C. git 上下文（查询权）
- 分支 feat/TAG0028-subagent-liveness；P0~P6 commit：999c672(P0) → b6581e3(P1) → d120453/2f1d887(P2) →
  50d48c6(P3) → 6964dbf(P4) → 34366ab(P4-fix3) → 700f074(P5) → <P6 commit>
- 事件账本 gate-events.jsonl 可交叉核对

### D. 说明
- 本任务 judge.enabled: true（.state.yaml），P6.5 强制
- P6 已判 33 PASS——你独立重验，不受其影响（不读 P6-acceptance.md）
</objective_info>

> 注：该文件禁止包含 PASS/FAIL 预判——否则被 `check-p6-provenance.py` 审计失败。
