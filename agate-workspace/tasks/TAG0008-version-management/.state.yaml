task_id: TAG0008
phase: P6
status: in_progress
retries:
  P1:
    - round: 1
      failure_mode: quality
      prompt_changed: true
      adjustment: "requirements-review needs-revision → analyst rev2 修复 9 项 → 复核 approved"
  P2:
    - round: 1
      failure_mode: empty_return
      prompt_changed: true
      adjustment: "architect 两次空返回（调研+最小验证完成但 P2-design.md 未落盘，认知过载）；换新会话 + dispatch-context 内联已验证设计输入，降低'从零写大报告'推理复杂度"
  P4:
    - round: 1
      failure_mode: quality
      prompt_changed: true
      adjustment: "P4 评审 rejected（review 专家 3 CRITICAL：指针解析 isdir 短路 / install-offline 无 Pillow bundle 默认流失败 / manifest 路径穿越）；回派 implementer 修复 3 CRITICAL 后重新评审"
