---
phase: P6.5
generated_by: 主 Agent（P6.5 不走 agate-inject-card.py，卡片 hash 校验不强制，见 dispatch-protocol.md「Judge 信息隔离」）
task_id: TAG0025
role: judge
round: 2
---

<dispatch_guide>
> ⚠️ 以下派发指引是本次任务的强制指令，不是参考信息。本文件严格遵循「Judge 信息隔离」白名单规则
> （dispatch-protocol.md），『输入文件』『上游关联』两节只含白名单项，不含 P6-acceptance.md /
> P4-implementation.md / P4-review.md / P5-test-results/ / 任何 P4-P6 的 dispatch-context 文件，
> 不含任何行首 `- PASS`/`- FAIL` 验收结论预判。

### 目标
本次是 P6.5 judge **第 2 轮**独立复核（第 1 轮判 needs-revision，BDD-10 FAIL，其余 15 条 PASS；
第 1 轮 verdict 已因格式问题移至 `.archived/round1-judge/`，不在你的白名单输入范围内，你不需要
也不应该读取它）。以全新 fresh context 逐条重验 TAG0025 全部 16 条 BDD（P1-requirements.md 定义），
不挑验、不信任何实现者/验收者自述，只凭 `P1-requirements.md` 标准 + `P2-design.md`（仅验收相关
节）+ `P6-evidence/` 证据文件（第 2 轮产出，已随 commit `b804cd8` 落库）+ git log 判案。产出
`P6.5-judge-verdict.md`。

### 约束

1. **逐条重验所有 16 条 BDD**：不挑验，含"看起来应该是 PASS"的也要独立核实证据是否真的支持。
2. **只信证据文件与 git log**：`P6-evidence/` 目录下的 16 个文件（第 2 轮产出）是你唯一可信的
   证据来源；git log 可用于核实 commit 存在性/author/时间线。不读、不引用 `P6-acceptance.md`。
3. **BDD-10 重点核实**：P1-requirements.md 第 4 节 BDD-10 与第 3.2 节现在已正式列出 **6 类**
   豁免（含新增第 6 类：`agate/tests/regression/test_repo_url_no_stale_rename.py` 自身，
   含第二处 `[BASELINE_CHANGE]` 标注）。请核实：① P1 原文是否真的已正式授权这 6 类（不是
   "暗示应该豁免"而是有明确的豁免清单条目 + BASELINE_CHANGE 标注）② `P6-evidence/` 里 BDD-10
   对应的证据文件是否按这 6 类豁免重新给出了"剩余命中数为 0"的可验证结果 ③ 可用 dispatch-context
   授予的命令核实权，独立重跑一遍全仓残留扫描命令（应用这 6 类豁免），交叉核对证据文件的结论。
4. **每条结论必须引用证据路径**：格式 `- (PASS|FAIL|NEEDS-REVISION) BDD-NN: {描述}
   ({P6-evidence/证据文件})`。
5. **证据不足 → 判 FAIL 或 NEEDS-REVISION，不得"看起来没问题"就放行**。
6. **三档预算**：复核轮次 ≤2（本次是第 2 轮，即最后一轮，超限交人工）、token ≤100k、时间
   ≤30min。

### 上游关联（白名单，仅含以下项）

- `P1-requirements.md`（16 条 BDD 标准，逐条判案唯一依据，重点 BDD-10 现含 6 类豁免正式授权）
- `P2-design.md`（仅验收相关节：`## 2. 实现完成的标志`）
- `P6-evidence/`（16 个证据文件，第 2 轮产出，本次判案的唯一证据来源）
- `.state.yaml`（任务状态，`judge.rounds` 当前为 2）
- `gate-events.jsonl`（事件账本）
- git log 查询权（非路径限制）

### 输入文件（按顺序读，仅限以下路径，不得访问白名单外文件）

1. `agate-workspace/tasks/TAG0025-agateon-rename/P1-requirements.md`
2. `agate-workspace/tasks/TAG0025-agateon-rename/P2-design.md`（仅 `## 2. 实现完成的标志` 节）
3. `agate-workspace/tasks/TAG0025-agateon-rename/P6-evidence/`（全部 16 个文件）
4. `agate-workspace/tasks/TAG0025-agateon-rename/.state.yaml`
5. `agate-workspace/tasks/TAG0025-agateon-rename/gate-events.jsonl`

### 产出路径

`agate-workspace/tasks/TAG0025-agateon-rename/P6.5-judge-verdict.md`（Header
status/criteria_total/criteria_passed/verdict_evidence[/partial] + 逐 BDD 结论）。**结论行
必须严格用纯文本格式 `- PASS BDD-N: ...` / `- FAIL BDD-N: ...` / `- NEEDS-REVISION BDD-N: ...`
——行首不要用 Markdown 加粗（`**FAIL BDD-N**`）包裹 PASS/FAIL/NEEDS-REVISION 关键字或 BDD 编号，
否则会导致 `check-judge-verdict.py` 解析失败（这是第 1 轮的教训，本轮务必用纯文本格式）。**
</dispatch_guide>

> 注：本文件已按白名单规则自查，不含黑名单路径引用，不含行首 PASS/FAIL 预判。
