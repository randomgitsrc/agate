---
status: needs-revision
criteria_total: 16
criteria_passed: 15
verdict_evidence: ["P6-evidence/bdd-1-readme-en.txt", "P6-evidence/bdd-2-readme-zh.txt", "P6-evidence/bdd-3-changelog.txt", "P6-evidence/bdd-4-install-sh.txt", "P6-evidence/bdd-5-agate-install-py.txt", "P6-evidence/bdd-6-agate-changes-py.txt", "P6-evidence/bdd-7-readme-md.txt", "P6-evidence/bdd-8-readme-zh-cn-md.txt", "P6-evidence/bdd-9-atomic-commit.txt", "P6-evidence/bdd-10-residual-scan.txt", "P6-evidence/bdd-11-confirmation-record.md", "P6-evidence/bdd-12-301-redirect.txt", "P6-evidence/bdd-13-ls-remote.txt", "P6-evidence/bdd-14-github-search.txt", "P6-evidence/bdd-15-remote-config.txt", "P6-evidence/bdd-16-fetch-verify.txt", "P1-requirements.md"]
partial: false
---

# P6.5 judge 独立复核结论（TAG0025，fresh context，零挑验）

逐条结论（P1 全部 16 条 BDD，含 P6 已判 PASS 项独立重验）：

- PASS BDD-1: README.md 首屏含 "Agateon (formerly agate)" (P6-evidence/bdd-1-readme-en.txt) —
  `head -15 README.md` 第 3 行原文 `> **Agateon (formerly agate)** — this project has a new
  name...`，落在文件前 15 行（首屏不滚动区），新旧品牌名同时出现，与 BDD-1 Then 子句字面一致。

- PASS BDD-2: README.zh-CN.md 首屏同时含 "Agateon" 与 "agate" 两个品牌词 (P6-evidence/bdd-2-readme-zh.txt) —
  第 3 行 `> **Agateon**（原名 agate）——本项目已改名...`，两词同时出现于首屏区域，满足 BDD-2
  "不要求逐字照搬英文版句式,但两个品牌词缺一不可"的宽松匹配要求。

- PASS BDD-3: CHANGELOG.md 新增 [Unreleased] 段并含 TAG0025 条目 (P6-evidence/bdd-3-changelog.txt) —
  `grep -n '^## \[Unreleased\]'` 命中文件第 11 行，`grep -n TAG0025` 命中第 13 行
  "### 变更（TAG0025：Agateon 品牌改名执行 Phase 0-1...)"；sed 1,20p 显示 Unreleased 段紧跟文件
  头部说明区（第 9 行 `---` 分隔线之后），结构上位于顶部，与 BDD-3 Given（原无 Unreleased 段，
  最新已发布段是 [0.63.0]）+ Then（顶部新增该段且含 TAG0025 条目）一致。证据窗口未直接显示
  [0.63.0] 段本身位置（sed 只截至第 20 行），但 Given 已确立 [0.63.0] 此前即为顶部已发布段，
  本次只做插入、无证据显示发生重排，可合理推定 Unreleased 段位于其上。

- PASS BDD-4: install.sh:24 指向新仓名 (P6-evidence/bdd-4-install-sh.txt) —
  `git clone https://github.com/randomgitsrc/agateon.git "$INSTALL_DIR"`，
  HAS_NEW_URL: yes / HAS_OLD_URL_RESIDUE: no。

- PASS BDD-5: agate/scripts/agate-install.py:55 指向新仓名 (P6-evidence/bdd-5-agate-install-py.txt) —
  `DEFAULT_REPO_URL = "https://github.com/randomgitsrc/agateon"`，HAS_OLD_URL_RESIDUE: no。

- PASS BDD-6: agate/scripts/agate-changes.py:116 指向新仓名 (P6-evidence/bdd-6-agate-changes-py.txt) —
  `"https://github.com/randomgitsrc/agateon.git"`，HAS_OLD_URL_RESIDUE: no。

- PASS BDD-7: README.md 第 5/29 行（badge + 安装入口）同批指向新仓名 (P6-evidence/bdd-7-readme-md.txt) —
  两行均含 `randomgitsrc/agateon`，均 HAS_OLD_URL_RESIDUE: no，未出现"只改一行"的部分修复。

- PASS BDD-8: README.zh-CN.md 第 5/29 行同批指向新仓名 (P6-evidence/bdd-8-readme-zh-cn-md.txt) —
  同上，两行均满足。

- PASS BDD-9: Phase 1 核心 7 处更新落在同一 commit (P6-evidence/bdd-9-atomic-commit.txt + 独立
  git 核实) — 证据文件内"per-file 最近 commit SHA"表对 install.sh / agate-install.py /
  agate-changes.py / README.md / README.zh-CN.md 五个文件均给出同一 SHA
  `751f421a4c36becd657ab12fed0e80cd7423bef3`；但证据文件末尾附带的 `diff --stat` 输出被截断，
  只显示到 `P4-progress.md` 一行为止，未见 install.sh/agate-install.py/agate-changes.py 三个
  脚本文件条目，与前述 SHA 归属表面冲突。本 judge 依据 dispatch-context 授予的 git 核实权，
  独立执行 `git show --stat 751f421a4c36becd657ab12fed0e80cd7423bef3`（未截断），确认该 commit
  的完整 diff --stat 确实包含 `install.sh | 2 +-`、`agate/scripts/agate-install.py | 2 +-`、
  `agate/scripts/agate-changes.py | 2 +-`（连同 README.md / README.zh-CN.md / CHANGELOG.md），
  14 files changed 全部同批。证据文件本身的截断不代表批次不原子，独立 git 核实排除了这一疑点，
  判 PASS。

- **FAIL BDD-10**: 全仓无旧仓库 URL 残留（含显式豁免清单）(P6-evidence/bdd-10-residual-scan.txt
  + P1-requirements.md BDD-10 原文) —
  P1-requirements.md 第 4 节 BDD-10 明确列出**仅 5 类**豁免范围（① archived/ 与
  agate-workspace/tasks|archived/ ② agateon-trademark-research.md ③
  2026-08-15-docs-suite-review.md ④ HANDOFF-TAG0025.md ⑤ design-rename-execution.md），并要求
  "排除后剩余命中数为 0"。P6-evidence/bdd-10-residual-scan.txt 提交的主证据是 pytest 用例
  `test_bdd_10_repo_wide_residual_scan_zero_after_exemptions` 的 PASSED 结果，但该证据文件自身
  同时附带一段标注为"reference (known blind spot, not BDD-10 evidence)"的原始 grep 命中列表，
  显示 `agate/tests/regression/test_repo_url_no_stale_rename.py` 第 6/9/38/92/195/286 行
  字面含 `randomgitsrc/agate`，且该文件路径不属于 P1 BDD-10 声明的 5 类豁免中的任何一类。
  本 judge 用 dispatch-context 授予的命令核实权，独立复跑 P2-design.md
  `gate_commands.P5_bdd10_residual_scan` 官方 gate 命令原文（含其排除正则），对当前仓库真实
  状态执行，结果 exit code 1（非零，gate FAIL），残留命中即为上述 agate/tests/regression/
  test_repo_url_no_stale_rename.py 内的字面旧 URL 引用——该官方 gate 命令的排除正则里，P2
  architect 额外追加排除了 5 个核心文件路径本身（P6-evidence 文件"reference"节脚注已引用测试
  文件顶部注释确认此点），但从未涵盖这个新增测试文件路径。
  pytest 证据之所以能 PASSED，是因为该测试文件内部 `_is_exempt()` 函数（测试源码第 90-99 行）
  额外增加了"文件自我豁免"（`rel_posix == self_rel_posix` 时直接豁免）这一**第 6 类豁免**，
  该类豁免既不在 P1-requirements.md BDD-10 原文的 5 类豁免清单内，P1-requirements.md 全文也
  只有一处 `[BASELINE_CHANGE]` 标注（对应第 5 类 design-rename-execution.md 的追加，已按"P1
  基线保护协议"走完主 Agent 核实批准流程），未见任何针对这个新增测试文件的第二处
  `[BASELINE_CHANGE]` 标注或对应的基线更新记录。
  结论：按 P1-requirements.md BDD-10 原文字面授权的 5 类豁免清单执行残留扫描，当前仓库真实
  状态下剩余命中数**不为 0**（命中于新增测试文件自身），且用于证明"数为 0"的 pytest 证据依赖
  一个未经基线变更协议批准的第 6 类豁免。BDD-10 判 **FAIL**——这是一个可修复的具体缺口（可选
  路径：为该测试文件走正式 BASELINE_CHANGE 流程把其自身路径正式补入 P1 BDD-10 豁免清单，或
  重写测试文件使其不再以字面字符串形式包含 `randomgitsrc/agate`），不构成任务方向性错误。

- PASS BDD-11: GitHub 仓库改名执行前获得用户在场放行确认 (P6-evidence/bdd-11-confirmation-record.md) —
  本条 BDD 性质上是治理类前置条件（会话内人工确认事件），非可重放的程序化命令，P6.5
  dispatch-context 已明确认可此条以"人工记录复核"形式提交证据。证据引用 env-rename-handoff.md
  「六、版本记录」原始记录："用户当次会话内明确放行确认后，主 Agent 亲自执行 `gh api -X PATCH
  repos/randomgitsrc/agate -f name=agateon`"，时序表述为"确认→执行"而非"执行→事后补记"，与
  BDD-11 Then 子句要求的时序一致；且与 P0-brief/P1-requirements 对"权限核实"与"放行确认"两个
  独立前置条件的表述交叉核对无矛盾。判 PASS（证据形式为该类治理性 BDD 的合理证据上限，本 judge
  无法对"确认事件是否真实发生于历史会话"做独立重放核实，但记录本身内部一致、无自相矛盾迹象）。

- PASS BDD-12: 旧仓库 URL 301 跳转新仓 (P6-evidence/bdd-12-301-redirect.txt) —
  `curl -sI https://github.com/randomgitsrc/agate` 响应 `HTTP/2 301` +
  `location: https://github.com/randomgitsrc/agateon`，响应头含完整 GitHub 真实响应特征
  （x-github-request-id / set-cookie / CSP 等），与 BDD-12 Then 子句一致。

- PASS BDD-13: `git ls-remote` 新仓名返回正常结果 (P6-evidence/bdd-13-ls-remote.txt) —
  `git ls-remote https://github.com/randomgitsrc/agateon.git HEAD` 返回
  `c1e889f9d2c77c730922ae440c5f56a2c4744ba0 HEAD`，EXIT_CODE: 0。该 SHA 前 7 位 `c1e889f`
  与本次任务分支 git 历史（`git log`/gitStatus 起始快照）中真实存在的
  "Merge pull request #209 from randomgitsrc/feat/feat-consistency-check-13" 提交一致，
  交叉验证该 SHA 非臆造，判 PASS。

- PASS BDD-14: GitHub `in:name agateon` 搜索首屏命中新仓 (P6-evidence/bdd-14-github-search.txt) —
  `gh api search/repositories -f q='agateon in:name'` 返回结果首位即
  `randomgitsrc/agateon`，符合 BDD-14 Then 子句"首屏可见"要求。

- PASS BDD-15: 主 checkout 迁移，worktree 自动跟随 (P6-evidence/bdd-15-remote-config.txt) —
  主 checkout 与 worktree 的 `git remote -v` 均已显示
  `https://github.com/randomgitsrc/agateon.git`，且 worktree `git config --show-origin`
  指向主仓 `.git/config` 文件路径，佐证共享配置机制成立、无需在 worktree 内重复 set-url。

- PASS BDD-16: 迁移后各 worktree 验证性抽查 fetch 成功 (P6-evidence/bdd-16-fetch-verify.txt) —
  主 checkout 与 worktree 各执行一次 `git fetch`，
  `MAIN_CHECKOUT_FETCH_EXIT_CODE: 0` / `WORKTREE_FETCH_EXIT_CODE: 0`，均成功、无报错。

## 总体结论

16 条 BDD 中 15 条判 PASS，1 条（BDD-10）判 FAIL。`status: needs-revision`——BDD-10 是一个具体
可定位、可修复的验收锚缺口（新增回归测试文件自身引入的字面旧 URL 引用未被 P1 授权的豁免清单
覆盖，"官方 gate_commands.P5_bdd10_residual_scan" 命令在当前仓库状态下实测 exit 1），不构成
方向性错误，弹回 P6/P4 补齐即可（走 BASELINE_CHANGE 流程正式扩豁免清单，或改写测试文件避免
字面旧 URL 字符串）。BDD-9 证据文件本身存在截断导致的表面疑点，已经独立 `git show` 核实排除，
不计入缺口。预算内（复核轮次 1、全部 16 条已逐条独立验完，未超 token/时间预算）完成，
`partial: false`。
