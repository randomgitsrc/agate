---
phase: P6.5
generated_by: 主 Agent（P6.5 不走 agate-inject-card.py，卡片 hash 校验不强制，见 dispatch-protocol.md「Judge 信息隔离」）
task_id: TAG0025
role: judge
---

<dispatch_guide>
> ⚠️ 以下派发指引是本次任务的强制指令，不是参考信息。本文件严格遵循「Judge 信息隔离」白名单规则
> （dispatch-protocol.md），『输入文件』『上游关联』两节只含白名单项，不含 P6-acceptance.md /
> P4-implementation.md / P4-review.md / P5-test-results/ / 任何 P4-P6 的 dispatch-context 文件，
> 不含任何行首 `- PASS`/`- FAIL` 验收结论预判。

### 目标
以 fresh context 独立复核 TAG0025 的全部 16 条 BDD（P1-requirements.md 定义），逐条产出结论，
不挑验、不信任何实现者/验收者自述，只凭 `P1-requirements.md` 标准 + `P2-design.md`（仅验收相关
节）+ `P6-evidence/` 证据文件 + git log 判案。产出 `P6.5-judge-verdict.md`。

### 约束

1. **逐条重验所有 16 条 BDD**：即使某条在 P6 已判 PASS，你也要独立重新判定，不能因为"应该是
   PASS"就跳过验证。
2. **只信证据文件与 git log**：`P6-evidence/` 目录下的 16 个文件是你唯一可信的证据来源；
   git log 可用于核实 commit 存在性/author/时间线。不读、不引用 `P6-acceptance.md`（你没有
   被授权访问该文件，dispatch-context 也未提供其路径）。
3. **每条结论必须引用证据路径**：格式 `- (PASS|FAIL|NEEDS-REVISION) BDD-NN: {描述}
   ({P6-evidence/证据文件})`。
4. **证据不足 → 判 FAIL 或 NEEDS-REVISION，不得"看起来没问题"就放行**。
5. **三档预算**：复核轮次 ≤2、token ≤100k（`.state.yaml` 未覆盖，默认值）、时间 ≤30min。超限
   → `status: needs-revision` + `partial: true`，说明哪些条目未验完，不得静默 passed。

### 上游关联（白名单，仅含以下项）

- `P1-requirements.md`（16 条 BDD 标准，逐条判案唯一依据）
- `P2-design.md`（仅验收相关节：`## 2. 实现完成的标志` 一节列出了本任务的验收判据摘要，可参考）
- `P6-evidence/`（16 个证据文件，本次判案的唯一证据来源）
- `.state.yaml`（任务状态）
- `gate-events.jsonl`（事件账本，可交叉核对执行留痕）
- git log 查询权（非路径限制，可用 `git log`/`git show` 等命令核实 commit 存在性）

### 输入文件（按顺序读，仅限以下路径，不得访问白名单外文件）

1. `agate-workspace/tasks/TAG0025-agateon-rename/P1-requirements.md`
2. `agate-workspace/tasks/TAG0025-agateon-rename/P2-design.md`（仅 `## 2. 实现完成的标志` 节）
3. `agate-workspace/tasks/TAG0025-agateon-rename/P6-evidence/`（全部 16 个文件）
4. `agate-workspace/tasks/TAG0025-agateon-rename/.state.yaml`
5. `agate-workspace/tasks/TAG0025-agateon-rename/gate-events.jsonl`

### 产出路径

`agate-workspace/tasks/TAG0025-agateon-rename/P6.5-judge-verdict.md`（Header
status/criteria_total/criteria_passed/verdict_evidence[/partial] + 逐 BDD 结论，格式见角色
定义文件）
</dispatch_guide>

> 注：本文件已按白名单规则自查，不含黑名单路径引用，不含行首 PASS/FAIL 预判。
