---
phase: P6.5
generated_by: agate-inject-card.py + 主 Agent
task_id: TAG0027
role: judge
---

<dispatch_guide>
> ⚠️ 以下派发指引是本次任务的强制指令，不是参考信息。执行优先级：派发指引 > 客观查证信息 > 阶段卡片（参考规范）

### 目标

以 **fresh context** 逐条重验 P1 的 26 条 BDD（BDD-1~26，**零挑验含 P6 已判 PASS 项**），产出
`P6.5-judge-verdict.md`（status: passed / needs-revision / rejected）。只凭标准（P1 BDD）与
证据文件、git log 判案，不信任何实现者/验收者自述。

### ⚠️ 信息隔离（强制——白名单/黑名单）

**你的输入只允许以下白名单**（dispatch-context 只含这些路径，禁含任何黑名单项）：

| 输入 | 用途 |
|------|------|
| `P1-requirements.md` | BDD 标准，逐条判案的唯一依据 |
| `P2-design.md`（仅验收相关节 §5 BDD 映射）| 验收设计参照 |
| `P6-evidence/` 目录 | 证据文件（唯一信源）|
| `.state.yaml` | 任务状态 |
| `gate-events.jsonl` | 事件账本（执行留痕交叉核对）|
| git log（另授查询权）| 命令执行/commit 存在性/author 查证 |

**黑名单（禁读禁引）**：P6-acceptance.md（verifier 自述——防锚定）、P6/P5/P4-dispatch-context-*.md、
P4-implementation.md、P4-review.md、P5-test-results/、P3-test-cases.md、任何含行首
`- PASS/- FAIL` 验收结论的文件。

### 约束

1. **逐条重验所有 BDD**：P1 有 26 条（`#### BDD-NN:` 编号 BDD-1~26）→ verdict 必须 26 条独立
   结论（criteria_total=26）。
2. **只信证据与 git log**：每条结论依据 = P6-evidence/ 证据文件内容 + git log 可查证记录。
   证据不足 → 判 FAIL 或 NEEDS-REVISION，不猜不放过。
3. **每条结论引用证据路径**：`- (PASS|FAIL|NEEDS-REVISION) BDD-NN: {描述} ({证据路径})`，
   引用须在 verdict_evidence 清单内且文件真实存在非空。
4. **认知约束**：禁止"看起来没问题"式结论。
5. **exit2-resolution 复核（BDD-12 Fix C）**：TAG0027 运行中未产生真暂停（exit ∉ pass_set），
   无 exit2-resolution 文件——按 Fix C 语义，健康任务无 resolution 文件不要求文件（账本含
   正常通过 exit:2 事件是健康标志）；但若账本/git log 显示曾落盘 resolution 文件则须校验。
6. **judge 产出格式**（judge.md）：frontmatter status/criteria_total/criteria_passed/
   verdict_evidence（JSON list）/partial + 正文逐条结论行。
7. 不改进程/不改代码/不碰 git 历史；[PROD_NOT_TOUCHED]。

### 上游关联

（信息隔离：不提供实现者自述。仅 P1 BDD 标准 + 证据目录。）

### 输入文件（白名单——按需读）

1. `agate-workspace/tasks/TAG0027-orchestration-semantics/P1-requirements.md`（26 BDD——判案
   唯一标准）
2. `agate-workspace/tasks/TAG0027-orchestration-semantics/P2-design.md`（§5 BDD 覆盖映射，
   验收语义参照）
3. `agate-workspace/tasks/TAG0027-orchestration-semantics/P6-evidence/`（9 个证据文件）
4. `agate-workspace/tasks/TAG0027-orchestration-semantics/.state.yaml`（phase=P6 + judge 启用）
5. `agate-workspace/tasks/TAG0027-orchestration-semantics/gate-events.jsonl`（事件账本）
6. git log（worktree，查证 commit/执行留痕）

### 产出文件字段

产出 `P6.5-judge-verdict.md`（任务目录）。frontmatter 用 agate-md-field-set 写
（status/criteria_total/criteria_passed/verdict_evidence），不要手写。
</dispatch_guide>

<!-- AGATE_CARD_START -->
<!-- AGATE_CARD_END -->

<objective_info>
### A. 路径拓扑
- worktree 根 = `/home/kity/oclab/agateon/.worktrees/agate-TAG0027`（分支
  feat/TAG0027-orchestration-semantics）
- 任务目录 = `agate-workspace/tasks/TAG0027-orchestration-semantics/`
- 协议本体（验收对象，可经 git log/只读查看）= worktree 的 `agate/`

### B. 26 BDD 分组（判案导航，非结论）
- BDD-1~5：phases.yaml next/retreat/gate_subphase/gate_pass_exit + schema + S-1/S-2 加列一致性
- BDD-6~13：agate-next/advance（pass_set 判定/retreat/真暂停 resolution/P6 特例）+ judge
  Fix C + BDD-13 返回约定
- BDD-14~17：五模式锚点 + 数据面禁平台名 + md 注记 + 排查可追溯
- BDD-18~25：渲染时注入 + 审计双锚点 + CHECK 14/15 + 两路并存
- BDD-26：gate_pass_exit 与 check-gate 真实 return 一致

### C. git log 查证提示
- P4 实现 commit fcf3fd2（含 exit2fix 修复）；P5 fcf3fd2 p5_pass；P6 最新 commit（验收）
- 事件账本 gate_run exit:2 = 各 phase 正常通过码（exit2fix 语义），非暂停
</objective_info>

> 注：该文件禁止包含 PASS/FAIL 预判——否则被 `check-p6-provenance.py` 审计失败。
