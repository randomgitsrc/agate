# BDD-8 人工抽查记录（协议实际内容与职责声明表一致）

复核人：verifier subagent（P6 验收阶段）
复核时间：2026-08-19

## 抽查对象 1：WORKFLOW.md「## 平台适配」小节（L465-469）

抽查内容原文（`grep -n "## 平台适配" -A5 agate/WORKFLOW.md`）：
```
465:## 平台适配
466:
467:不同 Agent 平台的 subagent 机制不同。详见 `platform-notes.md`《各平台适配说明》——权威唯一来源，本文件不重复维护。派发协议的具体调用方式见 `dispatch-protocol.md` 的平台适配章节。
468:
469:---
```

对照职责声明表（P2-design.md §0）对 `WORKFLOW.md` 的条目：
- 唯一职责：「主流程入口——P0-P8 阶段总览、裁剪规则、核心原则、需求/验收机制骨架」
- 不承载：「具体派发操作细节、平台能力矩阵全文、门槛可执行判定命令」

结论：**相符**。该段落此前是"职责定位混乱"的实例之一（曾独立展开平台能力表格/坑位描述），现已收窄为一句话摘要 + 双指针（指向 platform-notes.md 权威源 + dispatch-protocol.md 调用方式章节），不再承载"平台能力矩阵全文"，与职责声明表"不承载"列一致；保留的摘要句属于"主流程入口...骨架"范畴内的合理指引，不越界。

## 抽查对象 2：dispatch-protocol.md「## 派发编排机制」小节（L466 起，工作量评估/五模式编排/并行规则等）

抽查内容原文摘录（`grep -n "## 派发编排机制" agate/dispatch-protocol.md` 定位于 L466）：
```
466:## 派发编排机制

> 本节是 subagent 派发编排的权威来源（TAG0014，RM-AG0016）：工作量评估、编排模式、并行规则全阶段适用。既有有效规则（输入/产出数量上限、拆分判据、T016/T026 教训、P7 例外、状态机不变）保留在本节下方，不分散到各阶段卡片。各阶段卡片的「按包拆分并行」节引用本节（仅保留阶段特定约束）。
```

对照职责声明表（P2-design.md §0）对 `dispatch-protocol.md` 的条目：
- 唯一职责：「派发操作层——可执行门槛判定命令、**派发编排机制（工作量评估/并行规则/回退处理）**、特殊事件恢复」
- 不承载：「平台能力矩阵本身（引用 platform-notes.md）、派发 prompt 模板全文（引用 dispatch-prompt.md）」

结论：**相符**。「派发编排机制」小节（工作量评估五维评级、五模式编排、并行规则、全阶段适用表）恰好是职责声明表显式列举在 dispatch-protocol.md 唯一职责范围内的内容（"派发编排机制（工作量评估/并行规则/回退处理）"），并非需要迁移出去的错误归属内容——该段落此前被 P0-brief/P1 判定为"职责定位混乱"疑点之一，经核对职责声明表后确认属于合理保留在此的内容（理由：职责声明表 M7 已把此节的权威地位显式写入表中，本节本身就是该文件应承载的核心职责，不是外来内容误入）。

## 总结

两处抽查（WORKFLOW.md 平台适配摘要归属、dispatch-protocol.md 派发编排机制归属）均核对完成，结论均为"与职责声明表口径一致"——前者已完成迁移收窄（内容移出，仅留摘要+指针），后者被职责声明表显式认定为合理保留（内容本就属于该文件职责范围，无需迁移）。BDD-8 判定为 PASS。
