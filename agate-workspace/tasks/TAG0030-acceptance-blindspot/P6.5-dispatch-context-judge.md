---
phase: P6.5
generated_by: agate-inject-card.py + 主 Agent
task_id: TAG0030
role: judge
---

<dispatch_guide>
> ⚠️ 以下派发指引是本次任务的强制指令，不是参考信息。执行优先级：派发指引 > 客观查证信息 > 阶段卡片（参考规范）

### 目标

P6.5 验收独立裁判（judge，fresh context 信息隔离）：逐条重验 P1-requirements.md 的
BDD-1~21（全部 21 条，零挑验），产出 `P6.5-judge-verdict.md`。只信证据与 git log，
不读 verifier/implementer 的自述与派发上下文。judge 复核通过 → 主 Agent 写 phase: P7。

### 输入白名单（只允许读这些）

1. `P1-requirements.md`（BDD 唯一基准）
2. `P2-design.md`（仅验收相关节）
3. `P6-evidence/` 目录（证据文件）
4. `.state.yaml`（judge.enabled 状态）
5. `gate-events.jsonl` + git log（执行留痕查证）

### 输入黑名单（禁止读：verifier/implementer 自述与派发上下文一律不看）

P6-acceptance.md / P6-dispatch-context-verifier.md / P5-test-results/ /
P4-implementation.md / P4-review.md / P4-dispatch-context-*.md / P6-progress.md

### 约束

1. **逐条重验所有 BDD**（含 P6 已判 PASS 项，零挑验）：criteria_total 必须等于 P1
   `#### BDD-NN:` 标题数（21）。
2. **每条结论引用 P6-evidence/ 下真实存在且非空**的证据文件（verdict_evidence 清单内）。
3. **禁止"看起来没问题"式结论**；git log 可查证执行留痕。
4. **三档预算**：轮次 ≤2 / token 100k / 时间 30min。任一耗尽 → 立即停止，按已验条目落盘
   verdict，必须 status: needs-revision + partial: true，禁止 status: passed 静默放行。
5. **产出格式**：`P6.5-judge-verdict.md` Header 含 status（passed/rejected/needs-revision）+
   criteria_total + criteria_passed + verdict_evidence [JSON 列表]（+ partial 可选）；
   正文每条 BDD 一行 `- (PASS|FAIL|NEEDS-REVISION) BDD-NN: 描述 (证据路径)`。
6. **无行首预判**：本文件（dispatch-context）与留痕禁止行首 `- PASS`/`- FAIL` 预判
   （provenance 审计拦截）——verdict 正文的 PASS/FAIL 是复核结论不是预判，允许。
7. **不启用子派发能力**（judge 类角色例外，TAG0028 §4.4）：judge 不派发任何 subagent，
   不调用子派发工具。
8. **命令超时兜底**：所有 bash 命令外层 timeout（grep 复核 `timeout 60s`；git log `timeout 30s`）。
9. **环境隔离**：只读验证 + 写 verdict 产出，不改任何协议文件，标记 `[PROD_NOT_TOUCHED]`。

### 上游关联

- P1-requirements.md（21 BDD）+ P6-evidence/（24 证据文件）+ git log（P1~P6 commit 链）
- judge 为 P6.5 强门槛子阶段（挂载于 P6→P7 转移），check-gate P6.5 = check-judge-verdict +
  check-events 双 exit 0
- .state.yaml judge.enabled: true（本任务强制 judge）

### 输入文件（按顺序读，严格白名单）

1. `agate-workspace/tasks/TAG0030-acceptance-blindspot/P1-requirements.md`（BDD 基准）
2. `agate-workspace/tasks/TAG0030-acceptance-blindspot/P2-design.md`（仅验收相关节，按需）
3. `agate-workspace/tasks/TAG0030-acceptance-blindspot/P6-evidence/`（证据目录，逐文件核）
4. `agate-workspace/tasks/TAG0030-acceptance-blindspot/.state.yaml`
5. `agate-workspace/tasks/TAG0030-acceptance-blindspot/gate-events.jsonl` + `git log --oneline`

> 角色定义由主 Agent 在派发 prompt 中注入，judge 读取，不在本文件内引用其路径——保持信息隔离白名单纯净。

### 产出文件字段

产出 `P6.5-judge-verdict.md` 到任务目录，frontmatter 按角色定义写（judge 角色 schema
自定 status/criteria_total/criteria_passed/verdict_evidence，不套用 agate-md-field-set
常规字段）。
</dispatch_guide>

<!-- AGATE_CARD_START -->
<!-- AGATE_CARD_END -->

<objective_info>
### A. 路径拓扑
- worktree 根 = `/home/kity/oclab/agateon/.worktrees/agate-TAG0030`
- 任务目录 = `agate-workspace/tasks/TAG0030-acceptance-blindspot/`
- commit 链：P1 25c81f6 → P2 ba40610 → P3 167a044 → P4 e39c897 + SELF-GATE 3c2d647 →
  P5 196aca8 → P6（最新）

### B. 本任务验收对象特性（judge 复核背景）
- 纯协议文档面改造：BDD 载体 = 协议条文 grep 锚词命中 + 断言审计测试全绿 + consistency 0 ERROR
- 21 条 BDD 分四 phase：Phase1 清理钩子条文（BDD-1~6）/ Phase2 人工体验节（BDD-7~9）/
  Phase3 plan-design-review 形态驱动（BDD-10~15）/ Phase4 视觉契约+DEBT 三连（BDD-16~21）
- 证据 = P6-evidence/ 下 bdd-N-anchor.txt（grep 命中行）+ assert-full.log + consistency.log +
  count-tests.log（共 24 文件）
- 既有技术验证（P5 已过）：断言审计 21/21、consistency 0 ERROR、count 1457(+21)

### C. 已知单点风险（judge 可查证）
- unit 片 1 预存 flaky（TAG0011 竞态）已登记 known-failures.md——与本任务 21 BDD 无涉，
  判据是 P6-evidence 锚词命中 + 断言审计全绿，不受影响
</objective_info>

> 注：该文件禁止包含 PASS/FAIL 预判——否则被 `check-p6-provenance.py` 审计失败。