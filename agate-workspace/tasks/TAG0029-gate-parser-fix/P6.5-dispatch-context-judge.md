---
phase: P6.5
generated_by: 主 Agent
task_id: TAG0029
role: judge
---

<dispatch_guide>
> ⚠️ 以下派发指引是本次任务的强制指令，不是参考信息。执行优先级：派发指引 > 客观查证信息 > 阶段卡片（参考规范）
> P6.5 信息隔离：只读白名单输入，只信证据与 git log。黑名单文件一律不读（P6-acceptance.md / 各 dispatch-context / P4-implementation.md / P4-review.md / P5-test-results/）。

### 目标
以 fresh context 逐条重验 P1 全部 9 条 BDD（含 P6 已判 PASS 项，零挑验），产出 `P6.5-judge-verdict.md`（Header status/criteria_total/criteria_passed/verdict_evidence + 正文 9 条结论行）。

### 约束
- 判案依据仅：P1-requirements.md（BDD 标准）+ P2-design.md 验收相关节 + P6-evidence/ 9 个日志 + .state.yaml + gate-events.jsonl + git log 查询权。
- 每条结论引用 P6-evidence/ 下真实存在非空文件（verdict_evidence 清单内）；证据不足 → FAIL 或 NEEDS-REVISION，不猜。
- 结论行格式：`- PASS BDD-N: 描述 (证据文件名)` / `- FAIL BDD-N: ...` / `- NEEDS-REVISION BDD-N: ...`。
- Header：status（passed 全过 / needs-revision 需修订 / rejected 拒绝）+ criteria_total=9（必须 == P1 `#### BDD-NN:` 数）+ criteria_passed（passed 时 == 9）+ verdict_evidence（9 证据清单 JSON 列表）+ agent=judge。
- 预算：轮次 ≤2 / 时间 30min；超限诚实降级（status needs-revision + partial true）。
- 三档预算内完成；不改代码不碰 git 历史。
</dispatch_guide>

### 上游关联
- P1-requirements.md：9 条 BDD 标准（BDD-1~9）。
- P6-evidence/：9 个验收证据日志（p6-bdd-1.log ~ p6-bdd-9.log）。
- .state.yaml：phase=P6，judge.enabled=true。
- gate-events.jsonl：执行留痕账本。

### 输入文件（白名单，仅以下）
- `agate-workspace/tasks/TAG0029-gate-parser-fix/P1-requirements.md`
- `agate-workspace/tasks/TAG0029-gate-parser-fix/P2-design.md`（仅验收相关节：§8 实现完成标志）
- `agate-workspace/tasks/TAG0029-gate-parser-fix/P6-evidence/`（目录：9 个日志）
- `agate-workspace/tasks/TAG0029-gate-parser-fix/.state.yaml`
- `agate-workspace/tasks/TAG0029-gate-parser-fix/gate-events.jsonl`
- git log 查询权（worktree 内 `git log` 只读）

<!-- AGATE_CARD_START -->
<!-- AGATE_CARD_END -->

<objective_info>
- worktree 根：`/home/kity/oclab/agateon/.worktrees/agate-TAG0029`。
- 产出路径：`agate-workspace/tasks/TAG0029-gate-parser-fix/P6.5-judge-verdict.md`。
- P1 BDD 数：9（criteria_total 必须为 9）。
- 单 judge（非 double-judge；risk=high 但证据链单一，主 Agent 判定单 judge 足够）。
- 注：本文件自身不得含行首 - PASS/- FAIL  verdict 预判（provenance 审计要求）。
</objective_info>
