# P6.5-dispatch-context-judge — TAG0023 验收独立裁判

> 派发对象：judge（P6.5 验收独立裁判，fresh context）。这是本轮的强制指令，不是参考信息。
> 任务目录：`{AGATE_WORKSPACE}/tasks/TAG0023-mechanism-checks/`

## 目标

以 fresh context 独立、逐条重验 P1-requirements.md 的**全部 13 条 BDD**（含已判 PASS 项，零挑验），只凭 P1 标准 + P2 验收相关节 + P6-evidence/ 证据文件 + .state.yaml + gate-events.jsonl + git log 判案。产出 `P6.5-judge-verdict.md`。

## 输入文件（白名单，仅限以下路径，不得读取任何其他文件）

1. `{AGATE_WORKSPACE}/tasks/TAG0023-mechanism-checks/P1-requirements.md`（BDD 标准，逐条判案唯一依据）
2. `{AGATE_WORKSPACE}/tasks/TAG0023-mechanism-checks/P2-design.md`（仅第 4 节「完成标准」，验收相关判据）
3. `{AGATE_WORKSPACE}/tasks/TAG0023-mechanism-checks/P6-evidence/`（证据文件目录，全部 9 个文件）
4. `{AGATE_WORKSPACE}/tasks/TAG0023-mechanism-checks/.state.yaml`
5. `{AGATE_WORKSPACE}/tasks/TAG0023-mechanism-checks/gate-events.jsonl`
6. git log 查询权（可用 `git log`/`git show` 核对 commit 存在性、author，不限路径）

## 信息隔离说明

以上白名单是本轮仅有的合法输入来源。角色定义文件（judge.md）已完整列出禁止读取的黑名单路径类别，你按角色文件执行即可，本节不重复列出黑名单文件名（避免在派发文件中出现黑名单路径字符串本身）。

## 产出

`{AGATE_WORKSPACE}/tasks/TAG0023-mechanism-checks/P6.5-judge-verdict.md`

Header 格式：
```
---
status: <passed|rejected|needs-revision>
criteria_total: 13
criteria_passed: <N>
verdict_evidence: ["bdd-1-pytest.log", "bdd-2-pytest.log", ...]
partial: false
---
```

正文：逐条 `- (PASS|FAIL|NEEDS-REVISION) BDD-NN: {描述} ({证据路径})`，13 条一个不少。

## 门槛

- 13 条 BDD 全部有独立结论（零挑验）
- 每条结论引用 P6-evidence/ 下真实存在的证据文件
- Header status 字段三值之一

## 返回给我

按角色文件要求：`File: <路径>` + `Status: <passed|rejected|needs-revision>`

<!-- AGATE_CARD_START -->
<!-- AGATE_CARD_END -->
