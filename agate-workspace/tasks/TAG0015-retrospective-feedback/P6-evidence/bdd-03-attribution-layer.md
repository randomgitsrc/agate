# BDD-3 证据：归因分层字段（机制缺口 / 执行错误 二选一）

文件：`agate/assets/templates/retrospective-template.md`

## Then 子句逐项核对

Then 要求：每条问题标注"归因层面：机制缺口 / 执行错误"二选一字段，模板对该字段做强制说明 +
示例（二值语义，不允许留空或标注"两者都是"）。

| 要求 | 实际（行号） |
|------|-------------|
| 强制字段声明 | L56 `> 每条问题条目**强制**标注 `归因层面: 机制缺口 / 执行错误` 字段（二选一，二值语义，` |
| 显式禁止"留空"/"两者都是" | L57 `> **不允许留空，不允许标注"两者都是"**）：` |
| 两值各自定义 | L58 `> - `机制缺口`：agate 协议/脚本/模板没有定义或覆盖到，导致问题发生（应修协议）`；L59 `> - `执行错误`：协议本身有定义，但执行时没有遵守（应修纪律，不是修协议）` |
| 示例 | L61-66 `**填写示例**：` 代码块，含"归因层面: 执行错误"实例 |

## 原文摘录

```
> 每条问题条目**强制**标注 `归因层面: 机制缺口 / 执行错误` 字段（二选一，二值语义，
> **不允许留空，不允许标注"两者都是"**）：
> - `机制缺口`：agate 协议/脚本/模板没有定义或覆盖到，导致问题发生（应修协议）
> - `执行错误`：协议本身有定义，但执行时没有遵守（应修纪律，不是修协议）

**填写示例**：
```
- 问题：P4 实现时误改了测试断言而非实现代码
  归因层面: 执行错误
  说明：implementer.md 已明确"让测试变绿不改测试"，本次是未遵守既有规则
```
```

## 判定

**满足**——字段名、二选一语义、显式禁止"留空"和"两者都是"、双值定义、填写示例五项要素
在模板文件「发现的问题」节（L54-68）全部齐备，逐字对照 Then 子句无缺项。
