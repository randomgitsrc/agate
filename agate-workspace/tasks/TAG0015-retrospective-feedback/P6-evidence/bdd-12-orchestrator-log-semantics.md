# BDD-12 证据：orchestrator-log 语义扩展为"决策 + 依据"

文件：`agate/state-machine.md`

## Then 子句逐项核对

Then 要求：规则文本更新为同时允许/要求记录"决策 + 简要依据"（依据 = 触发该决策的客观信号，
如 gate 输出摘要/BDD 编号/文件路径引用，不等同于展开的思考过程），且保留"不写思考过程 /
不写文件内容摘要 / 不写 subagent 返回原文"三项既有排除不变（可通过对照修改前后文本校验排除项
未被移除）。

## 实际文本（当前 HEAD，第 481 行，「orchestrator-log.md 防无响应」小节）

```
- 不写思考过程、不写文件内容摘要、不写 subagent 返回原文——只写决策、下一步和触发决策的简要依据
  （依据示例：gate 输出摘要 / BDD 编号 / 文件路径引用；不等同于展开的思考过程）
```

## 逐项核对

| Then 要求 | 实际 | 满足？ |
|-----------|------|--------|
| 三项既有排除原样保留（"不写思考过程"/"不写文件内容摘要"/"不写 subagent 返回原文"） | 三个短语逐字出现在同一句开头，未删改 | 是 |
| 新增"决策 + 简要依据"要求 | "只写决策、下一步和触发决策的简要依据" | 是 |
| 依据定义（gate 输出摘要/BDD 编号/文件路径引用，不等同于展开思考过程） | 括号内举例三种依据形式 + 明确排除"不等同于展开的思考过程" | 是 |
| 旧版限制性表述"只写决策和下一步"（无顿号，四字后接"和下一步"）不应残留 | 已被替换为"只写决策、下一步和…依据"（改为三项并列），旧四字组合不再以原形式存在 | 是 |

## 本轮独立 grep 核实

```
$ grep -n "简要依据" agate/state-machine.md
481:- 不写思考过程、不写文件内容摘要、不写 subagent 返回原文——只写决策、下一步和触发决策的简要依据（依据示例：gate 输出摘要 / BDD 编号 / 文件路径引用；不等同于展开的思考过程）
497:（持续追加、逐决策颗粒度，只写"决策+简要依据"不写展开的思考过程）；`P{n}-checkpoint.md` 是 L2

$ grep -n "只写决策和下一步" agate/state-machine.md
（exit 1，零命中）
```

## 判定

**满足**——三项既有排除逐字保留，新增"简要依据"分句明确定义依据的形式与边界（不等同于
展开思考过程），旧的窄表述"只写决策和下一步"已不存在（替换为三项并列表述），符合 Then
子句要求的"文本更新"且"排除项未被移除"双重校验。
