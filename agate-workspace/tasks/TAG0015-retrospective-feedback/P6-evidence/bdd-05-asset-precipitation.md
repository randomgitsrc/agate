# BDD-5 证据：项目资产沉淀强制追问

文件：`agate/assets/templates/retrospective-template.md`

## Then 子句逐项核对

Then 要求：每条可复用项标注两类去向之一（①回馈 agate，关联 BDD-7「## agate 反馈」节；
②项目资产沉淀，并注明具体沉淀位置）；模板强制包含追问句"本次产生的临时命令/脚本/经验，
哪些该沉淀为项目固定资产？沉淀到哪？"作为该节填写引导语（可 grep 追问句原文校验）。

| 要求 | 实际（行号） |
|------|-------------|
| 两类去向标注①回馈 agate | L44 `> ①**回馈 agate**（该做法值得沉淀进 agate 协议本体，关联下方「## agate 反馈」节）` |
| 两类去向标注②项目资产沉淀+具体位置 | L45-46 `> ②**项目资产沉淀**（该做法是项目特定的，注明具体沉淀位置，如 `Makefile`/`scripts/` 或 `agents.md`/`project.md`）` |
| 强制追问句原文 | L48-49 `**填写引导语（强制追问，撰写本节前必须先回答）**：\n本次产生的临时命令/脚本/经验，哪些该沉淀为项目固定资产？沉淀到哪？` |
| 与 BDD-7「## agate 反馈」节关联 | ①去向标注明确写"关联下方「## agate 反馈」节"，模板 L147 确实存在该节标题，指向有效 |

## 原文摘录

```
> 每条可复用项标注两类去向之一：
> ①**回馈 agate**（该做法值得沉淀进 agate 协议本体，关联下方「## agate 反馈」节）
> ②**项目资产沉淀**（该做法是项目特定的，注明具体沉淀位置，如 `Makefile`/`scripts/` 或
> `agents.md`/`project.md`）

**填写引导语（强制追问，撰写本节前必须先回答）**：
本次产生的临时命令/脚本/经验，哪些该沉淀为项目固定资产？沉淀到哪？
```

## 判定

**满足**——追问句逐字原文出现（本轮用 grep 核实原文完全匹配 P1 §4.1 BDD-5 要求的字符串），
两类去向均有明确定义与位置提示，去向①指向的「## agate 反馈」节确实存在（BDD-7），非悬空引用。
