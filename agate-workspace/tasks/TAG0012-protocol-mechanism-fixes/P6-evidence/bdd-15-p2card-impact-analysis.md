# 证据：BDD-15 — P2 卡新增「影响面梳理」强制节（RM-AG0013）

验收方式：直接打开 HEAD 下的实际协议文件读新增内容，逐条核对 P1-requirements.md 该 BDD 的 Then 子句语义（非关键词存在性检查）。

## Then 子句逐项核对

- Then「文件中存在一个新增小节」：`## 影响面梳理（强制节）` 独立二级小节，位置在「gate_commands 声明」之前 —— 满足。
- Then「要求 architect 在设计候选方案前做一次影响面梳理」：节首句「**写候选方案之前**先做影响面梳理——方案的取舍取决于它牵动多大面，先设计再补影响面等于反过来给方案找理由」，推进条件同步新增 `- [ ] 含「影响面梳理」节（改什么 / 不改什么 / 风险在哪 三部分齐全，且写在候选方案之前）` —— 时序要求（设计前）显式且被 checklist 强制，满足。
- Then「本方案的改动是否波及 P1 未列出的文件/模块」：三部分之 1「**改什么（Modify）**：逐文件/逐模块列出改动点 + 关联 BDD 编号；改动落点必须落到『哪个文件的哪个小节/函数』，不写『相关代码』这种模糊表述」+ 之 2「**不改什么（Not Modify）**：显式列出**看起来该改但决定不改**的文件/范围 + 理由…是 P4 implementer 判断范围边界的依据（避免『顺手改进』）」—— 波及范围的正反两面都覆盖，满足。
- Then「是否与既有类似机制冲突或重复」：三部分之 3「**风险在哪（Risk）**：每条风险配一条缓解措施；跨模块引用、双源同步（权威源 + 副本）、schema 变更、并发/资源竞争是高频风险项」—— 「双源同步（权威源 + 副本）」正是「与既有机制重复/冲突」的具体化，满足。
- Then「该节可被关键词 grep（如「影响面梳理」）命中」：标题逐字含「影响面梳理」，pytest 锚点 BDD-15 本轮独立实跑 PASSED —— 满足。
- 客观性要求（Then 未强制、增强项）：「梳理动作要有客观证据：grep/rg 命中清单、读过的消费方代码、既有 gate 脚本的校验口径——不是凭印象列」，并堵住 `follows_existing_pattern` 豁免口子。
- 三卡同源关系：节首明确「P0 卡「同类/影响面预判」给量级、P1 卡「同类扫描」给清单，P2 在这两者基础上做**候选方案级**的影响域分析，三处同源、逐级细化，不重复劳动」—— 与 BDD-1/BDD-4 无重复定义。

## 实际文件文本摘录（HEAD）

### `agate/phase-cards/P2-design.md` L105-116

```markdown
## 影响面梳理（强制节）

**写候选方案之前**先做影响面梳理——方案的取舍取决于它牵动多大面，先设计再补影响面等于反过来给方案找理由。P0 卡片的「同类/影响面预判」给量级、P1 卡片的「同类扫描」给清单，P2 在这两者基础上做**候选方案级**的影响域分析，三处同源、逐级细化，不重复劳动。

P2-design.md 正文必须含影响面梳理节，覆盖三部分：

1. **改什么（Modify）**：逐文件/逐模块列出改动点 + 关联 BDD 编号；改动落点必须落到"哪个文件的哪个小节/函数"，不写"相关代码"这种模糊表述
2. **不改什么（Not Modify）**：显式列出**看起来该改但决定不改**的文件/范围 + 理由。这一栏比"改什么"更容易漏，也是 P4 implementer 判断范围边界的依据（避免"顺手改进"）
3. **风险在哪（Risk）**：每条风险配一条缓解措施；跨模块引用、双源同步（权威源 + 副本）、schema 变更、并发/资源竞争是高频风险项

梳理动作要有客观证据：grep/rg 命中清单、读过的消费方代码、既有 gate 脚本的校验口径——不是凭印象列。P1 已声明 `follows_existing_pattern` 的任务同样要做（沿用既有模式不等于影响面为零）。

```

## 结论

**PASS** —— 强制节存在且要求写在候选方案之前，改什么/不改什么/风险在哪三部分覆盖 Then 的「波及 P1 未列文件」与「与既有机制冲突或重复」两问。
