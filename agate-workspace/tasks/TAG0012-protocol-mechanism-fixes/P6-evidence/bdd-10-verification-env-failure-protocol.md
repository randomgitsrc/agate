# 证据：BDD-10 — verification_env 节扩为「条件化 + 失败处理协议」（RM-AG0014 主体）

验收方式：直接打开 HEAD 下的实际协议文件读新增内容，逐条核对 P1-requirements.md 该 BDD 的 Then 子句语义（非关键词存在性检查）。

## Then 子句逐项核对

- Then「节内新增『验证失败后怎么办』的协议内容」：既有「verification_env 条件化」段之后新增 `**verification_env 失败处理协议**` 子节，开宗明义「环境验证失败时按以下四条规则处理，不靠临场判断」—— 满足。
- Then 问题 1「哪些验证失败属于『可通过重试解决』、哪些属于『不可重试需升级人工』（可验证/不可验证清单）」：规则 1 给出**二列判据表**——可重试＝端口占用/临时资源未就绪、依赖包缺失但可标准安装、网络瞬时抖动/服务未完全启动、配置路径或环境变量误设；不可重试＝权限/凭据缺失、平台原生能力不支持、需外部人工提供的账号/证书/生产访问、**机制误用型**（应声明 verification_env 却标 supplementable）。两类各带处理动作（预算内重试 / 立即升级人工且**不消耗**轮次预算）—— **完整回答**。
- Then 问题 2「单轮验证失败时是否要求批量处理多个假设（而非逐个假设单独起一轮）」：规则 2「**批处理要求**」——「单轮验证若同时存在 ≥2 个待验证假设，必须一次性列出并在同一轮内**批处理**验证完；不允许『改一个假设 → 单独起一轮验证 → 再改下一个』」，并附 TAG0009 11.7 小时教训 —— **明确回答「是」并给出触发阈值（≥2 个假设）**。
- Then 问题 3「止损轮次由谁判定、超过轮次后状态转移到哪（如转 PAUSED）」：规则 3「**止损轮次 = 2 轮**」+「与阶段 retry（`retries[Pn]`）**独立计数**，不新增 `.state.yaml` 字段——由**主 Agent** 在验证 subagent 的 dispatch-context 中手工记录『当前第几轮验证 + 历次已排除假设清单』作为轮次追踪」+「超过 2 轮仍未解决 → 状态转 **PAUSED**，落盘 `PAUSED-resolution.md`」—— 三问（数值 / 判定人 = 主 Agent / 转移目标 = PAUSED）**全部回答**；且回答了 P1 第 2 节隐含需求 4 要求的「两套计数关系」（独立计数，理由：避免环境重试用光阶段 retry 预算）。
- Then 问题 4「若任务已进入 READY 后才暴露环境相关问题，问题归属如何判定」：规则 4 给出**三条判据且有优先顺序**——① 源于本任务改动引入的环境依赖变化 → 判定本任务遗留，回 P4/P5 修复；② 源于环境本身外部变化（平台/工具链升级）→ 判定环境本身问题，登记 known_risks/roadmap，不重开本任务；③ 证据不足无法判定 → 默认按第 1 条处理（保守原则）—— **完整回答，含兜底档**。
- P1 约束「具体规则数值由 P2 architect 设计，本 BDD 只验证问题清单是否被完整回答」：四问逐条有可执行答案，非占位表述 —— 满足。
- 权威性声明：子节头写「本节是权威定义，P5/P6 卡片与 verifier.md 引用本节，不重复展开」，与 BDD-17/18/19/20 的引用式落地形成单一权威源。

## 实际文件文本摘录（HEAD）

### `agate/dispatch-protocol.md` L1004-1021

```markdown
**verification_env 失败处理协议**（本节是权威定义，P5/P6 卡片与 verifier.md 引用本节，不重复展开）：声明了 verification_env 的任务，环境验证失败时按以下四条规则处理，不靠临场判断。

1. **先分类：可重试 / 不可重试**

   | 类别 | 判据 | 处理 |
   |------|------|------|
   | **可重试**（环境本身可通过标准操作恢复） | 端口占用 / 临时资源未就绪；依赖包缺失但可用标准安装命令补齐；网络连接瞬时抖动 / 服务未完全启动；配置路径或环境变量误设（本任务范围内可修正） | 在下方「止损轮次」预算内重试 |
   | **不可重试**（当前环境内本质无解） | 权限 / 凭据缺失且当前环境无法自行获取；平台原生能力不支持（如声明只在 Linux 可行的能力，在 Windows CI matrix 侧本质不可行）；需要外部人工提供的账号 / 证书 / 生产访问；**机制误用型问题**（如应声明 verification_env 却标了 supplementable——这是协议使用错误，应立即改正声明方式，不是"环境故障重试"） | 立即升级人工，**不消耗**验证轮次预算 |

2. **批处理要求**：单轮验证若同时存在 ≥2 个待验证假设，必须一次性列出并在同一轮内**批处理**验证完；不允许"改一个假设 → 单独起一轮验证 → 再改下一个假设 → 再起一轮"（TAG0009 教训：逐个假设串行试错导致环境验证拖了 11.7 小时）。

3. **止损轮次 = 2 轮**，与阶段 retry（`retries[Pn]`）**独立计数**，不新增 `.state.yaml` 字段——由主 Agent 在验证 subagent 的 dispatch-context 中手工记录"当前第几轮验证 + 历次已排除假设清单"作为轮次追踪。超过 2 轮仍未解决 → 状态转 **PAUSED**，落盘 `PAUSED-resolution.md` 引用本轮诊断（沿用既有 PAUSED 流程，不新建流程）。独立计数的目的：避免环境重试用光阶段 retry 预算，让实现质量问题仍有 retry 可用。

4. **READY 后问题归属判定（三条判据）**：任务已标 READY 后才暴露的环境相关问题，按以下顺序判定归属：
   1. 问题源于本任务改动引入的环境依赖变化（如新增依赖但 P0-brief 未声明）→ 判定**本任务遗留**，回 P4/P5 修复
   2. 问题源于环境本身的外部变化（与本任务改动无关，如平台/工具链版本升级导致行为差异）→ 判定**环境本身问题**，登记 `known_risks`/roadmap，不重开本任务
   3. 证据不足无法判定 → 默认按第 1 条处理（保守原则，避免真实缺陷逃逸）

```

## 结论

**PASS** —— 四个问题（可/不可重试分类、批处理、止损轮次判定人与 PAUSED 转移、READY 后归属）逐条有明确且可执行的答案，非泛泛提及。
