# 证据：BDD-12 —「派发编排机制」并行规则新增「资源密集型默认串行」判据（RM-AG0016）

验收方式：直接打开 HEAD 下的实际协议文件读新增内容，逐条核对 P1-requirements.md 该 BDD 的 Then 子句语义（非关键词存在性检查）。

## Then 子句逐项核对

- Then「该节新增第 4 条规则」：「4. 并行规则」原有 3 条（并行上限默认 3 / 失败批 retry / 共享文件统一后处理）之后新增 `4. **资源密集型默认串行**` —— 编号连续接在第 3 条后，满足。
- Then「即使批次间无数据依赖，若单批次任务本身是资源密集型…默认判定为串行」：规则首句「批次之间即使无数据依赖、无共享文件改动，只要命令属于**资源密集型**，默认改为串行（模式 5），不并行」—— 与既有 3 条的「数据依赖」维度正交，满足。
- Then「（如全量测试套件在 xdist 模式下跑、CDP/Playwright E2E 浏览器实例）」：判据四条命中任一即是——全量测试跑 xdist/多进程（`pytest -n auto`）、浏览器自动化 E2E（CDP / Playwright）、构建/打包/安装依赖类长命令、需独占外部资源的命令（同一端口 debug server / 同一测试数据库文件）—— 覆盖 Then 举的两类并有扩展，满足。
- Then「需评估 CPU/IO 竞争后才可改并行」：规则末「要并行跑资源密集型批次时，必须先按 P4 卡片「基础设施隔离（并行时强制）」为每批分配独立端口/数据库/临时目录，并在 dispatch-context 写明；无法隔离 → 串行（安全默认值）」；各判据行分别注明竞争对象（CPU 与文件锁 / 显存·端口·用户数据目录 / 磁盘 IO 与网络带宽）—— 「先评估资源竞争、可隔离才并行」的语义满足。
- Then「该规则是对既有「4. 并行规则」节的追加条目，不新建独立小节」：diff 显示改动是在既有 `### 4. 并行规则` 列表尾部 +1 项，全文无新增 `### 6.` 或同级新小节 —— 满足。
- Then「明确与既有「全阶段适用表」的 P5 行建立引用关系」：双向落地——① 规则末句「本条在「全阶段适用表」的 P5 行落地（…`gate_commands.P5` 常是全量测试/E2E，属资源密集型 → 默认串行），P5 卡片「按包拆分并行」引用本条」；② 「5. 全阶段适用表」P5 行同步改为「模式 2/3（按包拆批/并行）；资源密集型命令 → 模式 5（串行）」，说明列写「按「并行规则」第 4 条『资源密集型默认串行』处理」—— 双向引用成立，满足。

## 实际文件文本摘录（HEAD）

### `agate/dispatch-protocol.md` L735-741

```markdown
4. **资源密集型默认串行**：批次之间即使无数据依赖、无共享文件改动，只要命令属于**资源密集型**，默认改为串行（模式 5），不并行。资源密集型判据（命中任一即是）：
   - 全量测试套件跑 xdist / 多进程并发（如 `pytest -n auto`）——并行批次各自再起多进程会争抢 CPU 与文件锁
   - 浏览器自动化 E2E（CDP / Playwright）——各批次各起浏览器实例，显存/端口/用户数据目录易冲突
   - 构建/打包/安装依赖类长命令（编译、`npm install`）——磁盘 IO 与网络带宽是共享资源
   - 需要独占外部资源的命令（同一端口的 debug server、同一测试数据库文件）
   要并行跑资源密集型批次时，必须先按 P4 卡片「基础设施隔离（并行时强制）」为每批分配独立端口/数据库/临时目录，并在 dispatch-context 写明；无法隔离 → 串行（安全默认值）。本条在「全阶段适用表」的 P5 行落地（P5 虽是只读验证、无代码写冲突，但 `gate_commands.P5` 常是全量测试/E2E，属资源密集型 → 默认串行），P5 卡片「按包拆分并行」引用本条，不重复展开判据

```

### `agate/dispatch-protocol.md` L750-750

```markdown
| P5 | 模式 2/3（按包拆批/并行）；资源密集型命令 → 模式 5（串行） | 只读验证，无代码写冲突；但全量测试/E2E 命令按「并行规则」第 4 条"资源密集型默认串行"处理（见 P5 卡片） |
```

## 结论

**PASS** —— 第 4 条规则以追加条目形式落在既有并行规则节内，资源密集型判据可操作，且与全阶段适用表 P5 行双向建立引用。
