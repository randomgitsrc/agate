# 证据：BDD-4 — P1 卡新增「同类扫描」强制节（RM-AG0013）

验收方式：直接打开 HEAD 下的实际协议文件读新增内容，逐条核对 P1-requirements.md 该 BDD 的 Then 子句语义（非关键词存在性检查）。

## Then 子句逐项核对

- Then「文件中存在一个新增小节」：`## 同类扫描（强制节）` 独立二级小节 —— 满足。
- Then「明确要求 analyst 在识别隐含需求时执行一次全仓同类扫描」：第 1 条「扫描动作」写明「对问题涉及的关键符号（函数名、字段名、配置键、协议节标题、错误文案）用 grep/rg 扫**全仓**」—— 「全仓」范围明确，满足。
- Then「grep 关键概念/关键词」：同上，扫描对象列举到符号类型级 —— 满足。
- Then「核实影响面是否超出 P0-brief 已列范围」：第 2 条「逐条判定」要求每个命中标「本次处理 / 本次不处理 + 理由」，第 4 条要求结论落盘 P1-requirements.md 正文且「即使结论是『已确认只此一处』也要显式写出」；节尾写「同类扫描缺失 → requirements-review 打回」构成强制力 —— 满足。
- Then「该节可被关键词 grep（如「同类扫描」）命中」：标题逐字含「同类扫描」，pytest 锚点 BDD-4 本轮独立实跑 PASSED —— 满足。
- 与 BDD-1/BDD-15 的关系核对：节尾显式声明「P0 预判 / P1 同类扫描 / P2 影响面梳理，三处同源、逐级细化，不重复劳动」—— 三卡未重复定义，符合 agate「权威定义 + 引用」惯例。

## 实际文件文本摘录（HEAD）

### `agate/phase-cards/P1-requirements.md` L106-116

```markdown
## 同类扫描（强制节）

需求基线必须含一次**同类扫描**结论——被报告的那一处几乎从来不是唯一的一处。P0 卡片的「同类/影响面预判」给出粗粒度量级，P1 在此基础上把清单做实：

1. **扫描动作**：对问题涉及的关键符号（函数名、字段名、配置键、协议节标题、错误文案）用 grep/rg 扫全仓，记录**命中数量 + 文件清单**
2. **逐条判定**：每个命中标"本次处理 / 本次不处理 + 理由"。本次不处理的同类实例要么进 roadmap，要么写清为何不构成同一问题
3. **回归拦截**：若同类问题未来还会新增（不是一次性修完的存量），需求里要声明拦截手段（新增测试 / gate 脚本 / 文档约定），并转成对应 BDD
4. **结论落盘**：扫描结论写进 P1-requirements.md 正文（不是只写在 progress 里）；即使结论是"已确认只此一处"也要显式写出，空白不算做过

同类扫描缺失 → requirements-review 打回（"只修被报告的那一处"是 agate 反复复发的反模式）。P2 的「影响面梳理」在本节结论上继续做候选方案级的影响域分析，三处（P0 预判 / P1 同类扫描 / P2 影响面梳理）同源、逐级细化，不重复劳动。

```

## 结论

**PASS** —— 强制节存在，扫描动作/逐条判定/回归拦截/结论落盘四步齐全，缺失有打回后果，关键词可 grep。
