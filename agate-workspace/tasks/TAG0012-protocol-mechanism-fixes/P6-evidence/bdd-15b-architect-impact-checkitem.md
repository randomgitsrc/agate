# 证据：BDD-15b — architect.md「批次设计」节同步新增「影响面梳理」检查项（RM-AG0013）

验收方式：直接打开 HEAD 下的实际协议文件读新增内容，逐条核对 P1-requirements.md 该 BDD 的 Then 子句语义（非关键词存在性检查）。

## Then 子句逐项核对

- Given 复核：`agate/assets/execution-roles/architect.md` 改动前对「影响面」/「同类」关键词零命中（git show 27509a2 该文件 diff 仅 +7 行，全部为新增 checklist），改动后新增落点确在「批次设计（强制节）」内 —— Given 前提成立。
- Then「打开「批次设计」节（或语义最接近的既有节）…该节新增一个检查项」：「批次设计（强制节）」末新增 `**批次设计前置检查项**（拆批之前先过，缺任一项先补齐再拆）` 共 4 条，其中第 1 条即 `- [ ] **影响面梳理已完成**` —— 满足。
- Then「要求 architect 在设计候选方案前引用/执行 P2-design.md（BDD-15）定义的『影响面梳理』要求」：第 1 条写「批次边界必须建立在影响面梳理的『改什么 / 不改什么 / 风险在哪』三部分之上——没梳理清楚改动落点就拆批，会拆出跨批重复改同一文件的批次表。**要求见 P2 卡片「影响面梳理（强制节）」**」+ 检查项组前置句「拆批之前先过」—— 时序（先梳理后拆批）与引用关系均满足。
- Then「不在角色文件内重复展开梳理方法细节」：第 1 条只复述三部分名称并指向 P2 卡，未复制「梳理动作要有客观证据」等方法细节，末句显式写「本节不重复展开」—— 满足，与 BDD-19 verifier.md 的引用模式一致。
- Then「使 analyst.md/architect.md 两处角色文件对『同类/影响面』维度的覆盖方式保持对称」：analyst.md 侧 = 隐含需求清单新增维度条目 + 「落地要求见 P1 卡片「同类扫描」节」（BDD-7）；architect.md 侧 = 批次设计前置检查项 + 「要求见 P2 卡片「影响面梳理（强制节）」」（本条）—— 均为「角色文件放一条 checklist/维度 + 指向对应阶段卡权威节」，结构对称，满足。
- 关键词可 grep：条目含逐字「影响面梳理」，pytest 锚点 BDD-15b 本轮独立实跑 PASSED。

## 实际文件文本摘录（HEAD）

### `agate/assets/execution-roles/architect.md` L207-213

```markdown
**批次设计前置检查项**（拆批之前先过，缺任一项先补齐再拆）：

- [ ] **影响面梳理已完成**：批次边界必须建立在影响面梳理的"改什么 / 不改什么 / 风险在哪"三部分之上——没梳理清楚改动落点就拆批，会拆出跨批重复改同一文件的批次表。要求见 P2 卡片「影响面梳理（强制节）」，本节不重复展开
- [ ] **批次边界对齐影响面梳理的文件分组**：同一文件不跨批次被改两轮；跨批共享件（类型 / 接口 / 配置 / 权威源文档）单列，由主 Agent 在所有批次返回后统一处理
- [ ] **资源密集型批次已判定串行**：批次的 gate 命令属全量测试 xdist / E2E 浏览器 / 构建安装类时，默认串行（判据见 dispatch-protocol.md「派发编排机制」并行规则第 4 条"资源密集型默认串行"），要并行须先分配隔离参数
- [ ] **长命令已声明 `{key}_timeout_seconds`**：`gate_commands` 里耗时较长的 key（E2E / 构建 / 全量回归）按 per-key 形式声明预期耗时上限（如 `P5_e2e_timeout_seconds: 300`）。字段规则四点（排除 P3 / per-key 声明 / 三档默认基准表 / 缺字段向后兼容）的权威定义在 P2 卡片「gate_commands 声明」的 `{key}_timeout_seconds` 字段规则，本节只做声明位提醒，不重复展开基准表细节

```

## 结论

**PASS** —— 批次设计节新增前置检查项含「影响面梳理已完成」，以引用 P2 卡权威节的方式落地、不重复展开，与 analyst.md 侧覆盖方式对称。
