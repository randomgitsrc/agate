# 证据：BDD-17 — P5 卡「按包拆分并行」节引用「资源密集型默认串行」判据（RM-AG0016）

验收方式：直接打开 HEAD 下的实际协议文件读新增内容，逐条核对 P1-requirements.md 该 BDD 的 Then 子句语义（非关键词存在性检查）。

## Then 子句逐项核对

- Then「该节新增一句引用/补充」：「按包拆分并行」节内、既有「拆分判据同 P3。P5 是只读验证，无代码写冲突风险。」之后新增加粗段「**但『无写冲突』不等于可以随便并行**」—— 落点在指定节内，满足。
- Then「全量 pytest（尤其 xdist 并行模式）、frontend 全量 vitest、CDP/Playwright E2E 等资源密集型验证命令，默认判定为串行（即使包间无数据依赖）」：新增段写「`gate_commands.P5` 常是全量测试套件（含 xdist 多进程）或 E2E 浏览器命令，属**资源密集型默认串行**…即使包间无依赖也默认改为串行」—— 命令类型与「即使无依赖」条件均落地，满足。
- Then「与 `dispatch-protocol.md` BDD-12 的规则保持引用一致」：新增段写「按 dispatch-protocol.md「派发编排机制」并行规则**第 4 条**处理」；反向核对 BDD-12 规则末句亦写「P5 卡片「按包拆分并行」引用本条」—— 双向引用编号一致，满足。
- Then「不在本文件重复展开判据细节」：新增段只给结论 + 隔离前提（「要并行必须先按下方「基础设施隔离」为每批分配独立端口/数据库/临时目录，无法隔离即串行（安全默认值）」），并明写「判据细节见该节，本卡片不重复展开」；四条判据清单仍只在 dispatch-protocol.md —— 满足。
- 关键词可 grep：段内含逐字「资源密集型默认串行」，pytest 锚点 BDD-17 本轮独立实跑 PASSED。

## 实际文件文本摘录（HEAD）

### `agate/phase-cards/P5-verification.md` L122-125

```markdown
**但"无写冲突"不等于可以随便并行**：`gate_commands.P5` 常是全量测试套件（含 xdist 多进程）或 E2E 浏览器命令，属**资源密集型默认串行**——按 dispatch-protocol.md「派发编排机制」并行规则第 4 条处理，即使包间无依赖也默认改为串行；要并行必须先按下方「基础设施隔离」为每批分配独立端口/数据库/临时目录，无法隔离即串行（安全默认值）。判据细节见该节，本卡片不重复展开。

**环境准备职责边界（本阶段落地）**：verifier subagent **默认不自行启动环境**——debug server、测试数据库、临时端口等由主 Agent（或 P0-brief 声明的单一责任方）统一准备好，通过 dispatch-context 注入访问方式；多个并行 verifier 共享同一环境时更是如此，不允许各自启动。环境验证失败时的可重试/不可重试分类、批处理要求与止损轮次，一律按 dispatch-protocol.md「verification_env 失败处理协议」与「环境准备职责边界」执行，本卡片只做落地引用，不重复展开规则。

```

## 结论

**PASS** —— P5 卡在「按包拆分并行」节以引用 dispatch-protocol.md 并行规则第 4 条的方式落地资源密集型默认串行，未重复展开判据。
