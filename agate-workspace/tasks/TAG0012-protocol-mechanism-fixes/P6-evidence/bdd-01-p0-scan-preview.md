# 证据：BDD-1 — P0 卡新增「同类/影响面预判」节（RM-AG0013）

验收方式：直接打开 HEAD 下的实际协议文件读新增内容，逐条核对 P1-requirements.md 该 BDD 的 Then 子句语义（非关键词存在性检查）。

## Then 子句逐项核对

- Then「文件中存在一个新增小节」：`## 同类/影响面预判（强制，写在 known_risks 里）` 为独立二级小节，位于四字段说明与「环境自检」之间 —— 满足。
- Then「要求主 Agent 在填 `known_risks` 前先做一次同类/影响面预判」：节首句「填 `known_risks` 时不止列…还要先做一次**同类/影响面预判**」，且节标题即含「写在 known_risks 里」—— 满足。
- Then「本次改动历史上是否有过同类问题」：预判三问第 1 问「同类实例：这个问题/改动模式在仓库里还有没有别的实例？用 grep/rg 扫一遍关键符号…记命中数量 + 文件清单」—— 满足。
- Then「可能牵动哪些子系统/文件簇」：第 2 问「上下游消费方：被改动的对象有哪些消费方？改了之后谁会跟着变」+ 第 3 问「同类未来实例…拦截手段」—— 满足。
- Then「该节可被关键词 grep（如「同类」或「影响面预判」）命中」：标题逐字含「同类/影响面预判」，pytest 锚点用例 BDD-1 本轮独立实跑 PASSED —— 满足。
- 补充（Then 未要求但确认无副作用）：新增「推进条件」checklist 项一条，措辞为「哪怕结论是『已确认只此一处』」，不强制额外产出文件。

## 实际文件文本摘录（HEAD）

### `agate/phase-cards/P0-orchestrator.md` L29-48

```markdown
## 同类/影响面预判（强制，写在 known_risks 里）

填 `known_risks` 时不止列"这个改动有什么风险"，还要先做一次**同类/影响面预判**——问题很少只有一个实例，只修被报告的那一处是 agate 反复踩到的坑。

预判三问（结论写进 `known_risks`，哪怕结论是"已确认只此一处"）：

1. **同类实例**：这个问题/改动模式在仓库里还有没有别的实例？用 grep/rg 扫一遍关键符号（函数名、字段名、配置键、文案），把命中数量与文件清单记下来
2. **上下游消费方**：被改动的对象（字段/接口/文件/协议节）有哪些消费方？改了之后谁会跟着变
3. **同类未来实例**：这次是"修一处"还是"立一条规则"？若同类问题还会新增，`known_risks` 要注明需要防回归的拦截手段（测试 / gate 脚本 / 文档约定）

写法示例：

```yaml
known_risks:
  - "同类/影响面预判：grep `verification_env` 全仓命中 4 处（dispatch-protocol.md / verifier.md / P5 卡 / P6 卡），本次须同批处理，不能只改 1 处"
  - "同类/影响面预判：新增字段无运行时消费方，靠文档约定 + grep 断言测试兜底"
```

预判是 P0 的**粗粒度**版本，只要求给出量级和清单；P1 的「同类扫描」、P2 的「影响面梳理」在此基础上逐级细化（三处措辞不同但同源，见 P1/P2 卡片对应节）。

```

## 结论

**PASS** —— 新增小节存在、位置正确、三问内容覆盖 Then 要求的「历史同类问题」与「牵动哪些文件簇」两层，关键词可 grep 命中。
