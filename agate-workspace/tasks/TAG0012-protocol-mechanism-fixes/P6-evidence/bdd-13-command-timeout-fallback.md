# 证据：BDD-13 —「派发 prompt 模板」规范正文新增「命令超时兜底 + 命令前 progress」标准段（RM-AG0016）

验收方式：直接打开 HEAD 下的实际协议文件读新增内容，逐条核对 P1-requirements.md 该 BDD 的 Then 子句语义（非关键词存在性检查）。

## Then 子句逐项核对

- Then（规范正文，必改）落点核对：改动落在「派发 prompt 模板」节的内联模板正文（`## 分阶段落盘（留痕文件，防空返回）` 段与新增 `## 命令超时兜底（层级 4，所有 bash 命令强制）` 段），并在模板代码块之后新增 `### 命令超时兜底与既有超时机制的分层关系` 子节 —— 落在规范性主文本而非示例块，满足。
- Then 1「每条 bash 命令必须设置超时（超时时长 ≤ 该命令预期耗时的固定倍数，倍数由 P2 architect 定义具体值，本 BDD 只验证『倍数规则存在』）」：「执行任意 bash 命令前，必须设 shell 层 timeout，不允许无超时裸跑：`timeout {N}s {命令}`」+「取值 N = 该命令预期耗时 **×1.5**」，并分两种取值来源（已声明 `gate_commands.{key}_timeout_seconds` → 直接取该值；未声明 → 经验估算后 ×1.5）—— **倍数规则存在且给出具体倍数 1.5**，满足。
- Then 2「命令超时后 subagent 必须停止执行、写 progress 记录卡在哪条命令，返回主 Agent，不允许自行更换命令或深入诊断」：固定三动作——① 停止执行，不自行更换命令、不深入诊断、不重试第三次；② 往留痕文件写一行：卡在哪条命令、跑了多久、拿到什么输出；③ 返回主 Agent，由主 Agent 决定加长超时重跑 / 换策略 / 升级人工 —— 三项逐条对应，满足。
- Then 3「遇到非预期失败（非超时的报错）同样记录后返回主 Agent 判断，不允许 subagent 自行深入诊断」：触发条件写作「超时（**或出现非预期失败**）后的动作是固定的」，即非超时报错走同一套三动作 —— 满足。与既有「写脚本与跑脚本分离」节的「最小修复 vs 重写界限」核对：该节约束的是**主 Agent 跑脚本时**可做最小修复（改常量），本次新增约束的是**subagent 跑 bash 命令时**不得自行深挖，主体不同、无规则冲突，且新增段未重复展开「最小修复 vs 重写」判据（不重复定义成立）。观察记录：两节之间未互加显式跳转链接（BDD-13 第 3 点括注「互相引用」的字面形式未落地），但该点的规范性要求（非预期失败同样记录后返回、不自行深挖）与不冲突/不重复定义两项均已满足，故不据此判 FAIL。
- Then 4「分阶段落盘的粒度从『每个关键步骤』扩展到『每条 bash 命令执行前』」：`## 分阶段落盘` 段追加「落盘粒度不止『每读完一个输入文件』——**每条 bash 命令执行前**也追加一行（写清要跑什么、预期多久），这样命令挂死时主 Agent 从留痕文件就能看出卡在哪条命令上」—— 满足。
- Then 5「与 L790-879「Playwright/长时操作 subagent 派发策略」既有硬超时机制（层级 2）建立显式的文档内引用区分，明确标注本次新增的是『层级 4：bash 命令级超时兜底』」：新增 `### 命令超时兜底与既有超时机制的分层关系` 子节给出四层对照表（层级 1 = `{key}_timeout_seconds` 静态声明 / 层级 2 = 脚本内部硬超时 HARD 90s·180s，权威节指向本文件「Playwright/长时操作 subagent 派发策略」/ 层级 3 = `AGATE_TDD_TIMEOUT` / **层级 4** = bash 命令级兜底），并写「层级 2 是**内层**机制…层级 4 是**外层**兜底…二者叠加时，层级 4 的取值必须留够层级 2 完整走完的余量」—— 显式区分且给出叠加时的取值约束，满足；本节层级划分与 P1 第 0 节第 1 点的四层定义逐层一致。
- Then（L521 示例块，条件性）：self-gate/alignment-review 示例块之后新增「**为何本示例不展开「命令超时兜底」**」说明——判定该场景多为 grep/读文件/`git log` 秒级短命令、无长时挂起风险，故不重复展开；同时声明「纪律本身仍然适用（层级 4 是全局纪律）」并给出触发追加的条件（确要跑测试/构建/浏览器类长命令时须把模板该节一并追加）—— 属于条件性子句的「判定不适用 + 留一句为何不适用的说明」分支，未留空不处理，满足。
- 反例锚定：分层关系节末引 TPV0093 教训（`make test-quick` 无超时保护挂 188 分钟），与 P1 第 0 节第 1 点「层级 4 是本任务要补的空白」定性一致。

## 实际文件文本摘录（HEAD）

### `agate/dispatch-protocol.md` L462-484

```markdown
## 分阶段落盘（留痕文件，防空返回）
留痕文件：{AGATE_WORKSPACE}/tasks/{Txxx}/P{N}-progress.md
开始前先删除留痕文件（如已存在）：rm -f {AGATE_WORKSPACE}/tasks/{Txxx}/P{N}-progress.md
每读完一个输入文件或完成一个关键步骤，立即用 bash 追加一行（不要整理、不格式化）：
  echo "- [文件名] 关键发现摘要" >> {AGATE_WORKSPACE}/tasks/{Txxx}/P{N}-progress.md
不要在留痕文件里做内容整理、不格式化——那是产出文件的事。
读一个写一个，不要等全部读完再一次性写。
留痕文件仅供空返回诊断用，主 Agent 检查产出文件后可删。
落盘粒度不止"每读完一个输入文件"——**每条 bash 命令执行前**也追加一行（写清要跑什么、预期多久），
这样命令挂死时主 Agent 从留痕文件就能看出卡在哪条命令上。

## 命令超时兜底（层级 4，所有 bash 命令强制）
执行任意 bash 命令前，必须设 shell 层 timeout，不允许无超时裸跑：
  timeout {N}s {命令}     # 或工具自带的 timeout 参数
取值 N = 该命令预期耗时 ×1.5：
- 该命令对应的 `gate_commands.{key}_timeout_seconds` 已声明 → "预期耗时"直接取该值
- 未声明（含绝大多数非 gate 的日常 bash 调用）→ 按经验估算预期耗时，再 ×1.5
超时（或出现非预期失败）后的动作是固定的：
1. 停止执行，不自行更换命令、不深入诊断、不重试第三次
2. 往留痕文件写一行：卡在哪条命令、跑了多久、拿到什么输出
3. 返回主 Agent，由主 Agent 决定加长超时重跑 / 换策略 / 升级人工
"宁可等一等也不要无限挂起，宁可早返回也不要自己越权改命令"。

```

### `agate/dispatch-protocol.md` L517-535

```markdown
### 命令超时兜底与既有超时机制的分层关系

上面模板里的「命令超时兜底」是**层级 4**，与既有的几层超时机制职责不同，不要互相替代：

| 层级 | 机制 | 谁施加 | 权威节 |
|------|------|--------|--------|
| 层级 1 | 阶段 gate 命令的预期时长声明（`gate_commands.{key}_timeout_seconds`） | P2 architect 静态声明 | P2 卡片「gate_commands 声明」 |
| 层级 2 | 脚本内部硬超时（Playwright/Node 脚本 `HARD = 90_000` / `180_000`，exit 2 + `lastStep`） | 脚本自己 | 本文件「Playwright/长时操作 subagent 派发策略」的 subagent 超时判定 |
| 层级 3 | P3 测试运行器超时（`AGATE_TDD_TIMEOUT`，默认 120s，exit 124 → 超时 JSON） | 测试运行器 | `agate_common.py` 的 `run_test_with_formatter()` |
| **层级 4** | **bash 命令级超时兜底**：subagent 跑任意 bash 命令前设 shell 层 `timeout`，取值 = 预期耗时 ×1.5 | subagent 自己，每条命令 | 本节 + 上方派发 prompt 模板 |

区分要点：

- 层级 2 是**内层**机制，只覆盖那一个脚本自己的执行；层级 4 是**外层**兜底，覆盖 subagent 跑的每一条 bash 命令（含 `git`、`pytest`、构建命令等根本没有内部硬超时的命令）。二者叠加时，层级 4 的取值必须留够层级 2 完整走完的余量（如脚本 HARD=180s，外层 `timeout` 不能设 120s，否则外层先杀，拿不到 `lastStep` 诊断信息）
- 层级 1 是**静态声明**（P2 写在文档里，供人和 subagent 读），层级 4 是**运行时纪律**（subagent 每次执行前真的去设 timeout）。层级 1 声明了值，层级 4 就用它 ×1.5；没声明就按经验估算 ×1.5
- 层级 3 只服务 P3（TDD 红灯判定），与层级 1 的 `timeout_seconds` 互不覆盖（关系说明见 P2 卡片「gate_commands 声明」的 `timeout_seconds` 字段规则）

—— TPV0093 教训：`make test-quick` 无任何超时保护，实际挂了 188 分钟才被发现。层级 4 的存在就是为了让"挂死"在预期耗时 ×1.5 的时刻变成一条可诊断的返回，而不是无限等待。

```

### `agate/dispatch-protocol.md` L559-564

```markdown
**为何本示例不展开「命令超时兜底」**：self-gate 审查 / alignment-review 这类非阶段派发，跑的基本是
grep / 读文件 / `git log` 这类秒级短命令，没有长时挂起风险，逐条命令声明 ×1.5 超时只会稀释 prompt。
超时兜底纪律本身仍然适用（层级 4 是全局纪律，见上方「命令超时兜底与既有超时机制的分层关系」），
只是**不在本示例里重复展开**。若某次非阶段派发确实要跑测试/构建/浏览器类长命令，则须把上方派发
prompt 模板的「命令超时兜底」节一并追加进 prompt。

```

## 结论

**PASS** —— 规范正文含命令超时兜底标准段，五项 Then 子句（倍数规则 / 超时三动作 / 非预期失败同处理 / 落盘粒度到每条命令前 / 与层级 2 显式分层区分）全部落地，条件性子句以「判定不适用 + 说明理由」分支闭合。
