---
phase: P6.5
generated_by: agate-inject-card.py + 主 Agent
task_id: TAG0031
role: judge
---

<dispatch_guide>
> ⚠️ 以下派发指引是本次任务的强制指令，不是参考信息。执行优先级：派发指引 > 客观查证信息 > 阶段卡片（参考规范）
> 本次是第 2 轮（格式修正）：第 1 轮 `check-gate.py P6.5` 未通过，纯格式问题，不是判定内容有误。你的 15 条 BDD 独立判定结论（全部 PASS）保持不变，只修正两处格式。第 1 轮的修复尝试因 dispatch-context「输入文件」/「上游关联」两节引用了不在 P6.5 白名单内的文件名（`gate-diagnosis.md`）而被 `check-judge-verdict.py` 拦截——本轮已把全部修复细节直接写在本节「约束」里，「输入文件」「上游关联」两节只保留白名单内路径，不要在这两节之外的地方去读取或引用任何 gate-diagnosis 类文件。

### 目标
修正 `P6.5-judge-verdict.md`：① frontmatter `verdict_evidence` 从布尔值改为非空列表（15 个证据文件名）；② 正文 15 条结论行的证据引用括号从全角改半角，且括号内只留纯证据文件名（补充说明文字移到括号外）。**不要重新做判定**——15 条 BDD 的 PASS/FAIL 结论、独立验证依据的实质内容都不变，这只是把已有内容重新排版成机器可解析的格式。

### 约束（本次修复的完整依据，全部内联，不外部引用）
- **问题 1 — `verdict_evidence` 字段类型**：`check-judge-verdict.py` 要求 `verdict_evidence` 是**非空列表**，当前是布尔值 `true`（错误）。改为：
  ```yaml
  verdict_evidence:
    - bdd-01.log
    - bdd-02.log
    - bdd-03.log
    - bdd-04.log
    - bdd-05.log
    - bdd-06.log
    - bdd-07.log
    - bdd-08.log
    - bdd-09.log
    - bdd-10.log
    - bdd-11.log
    - bdd-12.log
    - bdd-13.log
    - bdd-14.log
    - bdd-15.log
  ```
  （文件名不带 `P6-evidence/` 前缀，脚本内部会自动拼接该目录。）
- **问题 2 — 正文结论行证据引用括号格式**：`check-judge-verdict.py` 的证据交叉核对只识别**半角 ASCII 圆括号 `()`**，且要求括号内**只能是**纯证据文件名（可逗号分隔多个），不能混入其他文字。当前正文用的是**全角括号（）**，且括号内混入了"独立重跑 X 项全 PASS"这类说明文字，导致解析器完全提取不到引用。逐条修正为：
  ```
  ＜STATUS＞ BDD-1: compute_sha256 迁移到 agate_common 后两侧结果一致，全仓仅 1 处定义——独立重跑 5 项
    全 PASS，独立 grep 确认仅 agate_common.py:583 一处 `def compute_sha256` (bdd-01.log)
  ```
  （上面示例中 `＜STATUS＞` 是占位符，代表原本该行的真实结论词 PASS/FAIL——本节故意不写字面结论词开头的行，避免触发 `check-judge-verdict.py` 的"dispatch-context 全文行首验收结论预判"扫描；你在 P6.5-judge-verdict.md 正文里当然要写回真实的 `- PASS BDD-N:`/`- FAIL BDD-N:`，那是产出文件的合法格式，不受此限制，只是本 dispatch-context 自身不能这样写。）
  即：把原本塞进全角括号里的"独立重跑 X 项 PASS / 独立读取 Y 文件确认 Z"这类说明文字，移到冒号后的描述部分（括号外，用破折号或逗号衔接均可）；行尾只留一个半角括号，括号内仅放证据文件名（如 `bdd-01.log`，`bdd-02.log` 等，与上面 `verdict_evidence` 列表一一对应，15 条结论行各自对应各自的 1 个证据文件）。
- **不改动**：判定内容（15/15 PASS）、`criteria_total`/`criteria_passed`/`status`/`partial` 字段值、「交叉核对」「总体判定」等其余章节内容——只改上述两处格式。
- **修复后验证**：跑 `cd /home/kity/oclab/agateon/.worktrees/agate-TAG0031 && timeout 30 python3 ~/.agate/scripts/check-gate.py P6.5 agate-workspace/tasks/TAG0031-debt-cleanup` 确认 exit 0。

### 上游关联
- 第 1 轮判定内容不变（15/15 PASS），本轮只做格式修正

### 输入文件
- {AGATE_WORKSPACE}/tasks/TAG0031-debt-cleanup/P6.5-judge-verdict.md（待修改的现有产出，用 Edit 工具原地修改）
</dispatch_guide>

<!-- AGATE_CARD_START -->
<!-- AGATE_CARD_END -->

<objective_info>
- 环境状态：worktree 分支 feat/TAG0031-debt-cleanup，无变化
- 关键标识：`check-judge-verdict.py` 的 dispatch-context 白名单扫描只认「输入文件」/「上游关联」两节内的 `p1-requirements.md`/`p2-design.md`/`.state.yaml`/`gate-events.jsonl`/`p6.5-judge-verdict.md`，其余文件名一律判定越界——本文件已确保这两节不含任何白名单外文件名
- 查证结果：主 Agent 已定位两处格式问题（完整修复方案见上「约束」节）
</objective_info>
