---
review_date: 2026-09-02
reviewer: independent-design-review
change_summary: subagent 存活可观测性与自主再派发设计 v2 复审——v1 FAIL（2 BLOCKER+3 WARNING+3 NIT）逐条修复后的复核
files_reviewed: [design-subagent-liveness-and-self-dispatch.md (v2), review-subagent-liveness-and-self-dispatch-v1-20260902.md]
resolution: v1 全部 BLOCKER/WARNING 已核验闭合，复审过程中发现的 2 处轻微排版问题（章节编号乱序 §3.5/§3.4 顺序颠倒、全角字符残留）已一并修复，不影响本次结论
---

# subagent 存活可观测性与自主再派发设计 v2 复审

审查对象：`docs/design-notes/design-subagent-liveness-and-self-dispatch.md`（214 行，v2，v1 独立评审 FAIL 后的修复版）。
复审方式：逐条核对 v1 评审报告列出的 B1/B2/W1/W2/W3 是否在 v2 中确有对应修复内容，并检查修复本身是否引入新的事实性错误或自相矛盾。

## 逐条闭环核验

| 编号 | v1 指出的问题 | v2 修复内容 | 核验结果 |
|------|--------------|------------|----------|
| B1 | 实测案例分析未区分"既有机制执行缺口"与"真正结构性空白"，可能夸大问题范围 | 新增 §1.5，明确定义可能性 A（P2 五维评级本该评 high 但未拆分执行，执行纪律问题）与可能性 B（评级已正确完成，subagent 执行中动态发现的顺序依赖粒度，结构性空白），并将本设计范围显式限定为可能性 B；§6 新增待确认事项 5，要求核实具体案例归类 | ✅ 闭合。区分逻辑清晰，且给出了后续核验路径（若归类为 A 需更换论证素材），不是空泛声明 |
| B2 | 子任务心跳文件落盘归属未定义（命名/审计豁免/清理时机缺失）| 新增 §3.4（原 §3.5，见下方排版问题）"心跳文件的生命周期定义"，覆盖命名规范（父子分层 `.heartbeat` / `.heartbeat.child-{n}`）、审计豁免（明确指出需要在对应脚本路径过滤逻辑显式确认，不能假设默认安全）、清理时机（比照既有 `agate-archive-stale-outputs.py` 模式）| ✅ 闭合。三个子问题均有对应条款，且"审计豁免"一条没有草率断言"肯定不会被扫到"，而是要求显式核验，符合评审对可核验性的要求 |
| W1 | "从未心跳"与"心跳过期"两种情况处理路径在正文与风险表不同步 | §3.2 判定逻辑改为三支（新鲜/过期/从未心跳），且"从未心跳"分支与 §5 风险表第一行的处理路径描述一致（均指向"视为派发失败，走既有重派流程，不当作执行卡死"）| ✅ 闭合，两处表述一致 |
| W2 | OpenCode CLI 路线未经实测验证，风险表未覆盖版本漂移风险 | §4.3 OpenCode 行新增"注"，明确标注未实测、需要实机验证；§5 风险表新增一行专门覆盖"opencode run 行为未经实测+随版本变化"风险 | ✅ 闭合 |
| W3 | judge 类角色例外声明依据"开放性冲突"表述笼统，未论证子派发本身是否必然破坏 fresh context | §4.4 补充完整论证链：区分"子任务本身是否 fresh context"与"决定派子任务查什么这个决策路径本身是否受主观判断影响"，指出后者才是与 judge 独立性冲突的关键点，并与普通执行角色做了对比说明为何普通角色不受此约束 | ✅ 闭合，论证具体到了决策路径层面，不再是笼统归因 |
| N1 | design-notes/README.md 未登记 | README.md 已新增本文档条目，含评审链状态描述 | ✅ 闭合 |
| N2 | 心跳周期默认值"10 秒/3 周期"在正文与待评审事项表述不一致（示意 vs 待定）| §3.2 代码示例标注 `AGATE_HEARTBEAT_INTERVAL`，§6 待评审事项 1 改为明确标注"候选默认值，非最终值"并要求确认后保持全文一致 | ✅ 闭合，不再有隐含的表述分歧 |
| N3 | 资源密集型阶段与心跳阈值交互未展开分析（原为遗留待办登记）| §6 待评审事项 2 保留并细化（提及可比照 `parallel_limit` 可覆盖默认值的既有模式），符合"NIT 可延后但需可追踪"的处理方式 | ✅ 按 NIT 级别处理，不要求本轮必须展开分析，符合评审惯例 |

## 复审中发现的修复副作用（非新增 BLOCKER/WARNING，已顺手修复）

1. **章节编号乱序**：v2 初版中 §3.4"心跳文件的生命周期定义"被插入在原 §3.4"影响面"之前但仍标号为 §3.5，导致文档内出现"§3.5 在 §3.4 之前"的乱序。已修正为 §3.4 生命周期定义 → §3.5 影响面的正确顺序。
2. **全角字符残留**：§4.4 论证段落中"决策路径選擇"含全角"選擇"，与全文简体表述不一致，已修正为"选择"。

以上两处不构成本轮评审的独立发现项（不影响 B1/B2/W1-W3 的实质性内容判断），作为复审附带的编辑级修正一并记录，不改变本次复审结论。

## 是否通过

**PASS**——v1 全部 BLOCKER（2）与 WARNING（3）均已核验确实闭合，N1-N3 按 NIT 级别妥善处理（登记或延后均在合理范围内），复审过程中的两处编辑级问题已随手修复且不影响结论。v2 可作为定稿版本提交立项评估。

> 遗留事项提醒（非阻断，供立项参考）：§6 待评审事项 4（RM 编号排期，与 RM-AG0054 存在 dispatch-protocol.md 改动面交叉）与待评审事项 5（TPV0095 案例归类核实）均为立项前需要人工确认的开放问题，不是设计文档自身的缺陷，PASS 结论不代表这两项已有答案。
