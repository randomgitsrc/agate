# agate v0.8 self-gate 实施评审（commit `8a55a1d`）

> 评审范围：self-gate 机制全部落地——CHECK 9 + 角色文件 + 派发模板 + 文档改动 + 测试
> 方法：跑完整测试套件、跑一致性检查工具、核对角色文件/派发模板/AGENTS.md 的内容、验证 CHECK 9 的真实输出

---

## 总体结论

**实现质量很高——162/162 bats 通过，CHECK 9 正确识别出已知的 md5 缺口（WARN），角色文件/派发模板/文档改动与计划逐条对应，SG.6 测试自动验证 11/11 脚本覆盖。但 CHECK 8 的 bats 测试仍然缺失（终审里专门提了"建议和 CHECK 9 的测试一起写"，这次 CON.8 这个编号被拿去给 CHECK 9 用了），另外测试计数在 4 个地方又出现了互相矛盾的数字。**

---

## 一、✅ 162/162 实测通过

```bash
$ bats agate/tests/sanity.bats agate/tests/unit/ agate/tests/regression/ agate/tests/integration/
1..162
pass: 162, fail: 0
```

commit message 声称"162 bats / 0 ERROR"——这次是真的。

---

## 二、✅ CHECK 9 行为验证

```bash
$ python3 agate/scripts/check-protocol-consistency.py --root .
  ✅ PASS  CHECK 8  v0.6 关键词存在性
  ⚠️  WARN  CHECK 9  协议-脚本结构对齐

  WARNING (2 条 CHECK 9 相关):
    ⚠️  P6 截图去重（md5）: 脚本 check-p6-evidence.sh 缺少关键词 'md5'
    ⚠️  P6 截图去重（md5）: 脚本 check-p6-evidence.sh 缺少关键词 '去重'
```

这正是计划在 WARN 分类表里标注"✅ 已加"的那唯一一条真锚点（#3 md5 去重），CHECK 9 正确报了 WARN。**17 个锚点里 15 个 PASS、1 个锚点的 2 个关键词都 WARN——行为完全符合预期。**

CON.9 测试还专门锁住了这个已知 WARN，防止有人删锚点来清 WARN 而不是真的实现 md5 去重——这种"防偷懒"的测试设计很好。

---

## 三、✅ 角色文件 + 派发模板 + 文档改动

逐项核对：

- `protocol-alignment-review.md`：frontmatter 完整（role_id/type/phases/agent），A1-A6 审查清单、审查原则、配套文件提示、输出格式模板、闭环规则（含 HUMAN_CONFIRMED）、人工验收清单——和计划 §3-4 节完全对应
- `dispatch-protocol.md`：补了完整的派发模板（含 diff 摘要占位、审查范围、配套文件提示、审查清单引用、产出路径），位置在文档末尾"派发协议"总结段之前
- `AGENTS.md`（repo-root）：补了"改 agate 协议本体的检查清单"5 步流程
- `LIMITATIONS.md`：局限 5 补了"缓解（v0.8）"说明
- `orchestrator-template.md`：Hardening 节补了 self-gate 说明

**所有 6 个 SG.* 集成测试也验证了上述内容的存在性和关键字段。**

---

## 四、🟠 CHECK 8 仍然没有 bats 测试

终审结论里写的是：

> 实施时唯一要补的历史债：CHECK 8 的 bats 测试（CON.8），建议和 CHECK 9 的测试一起写。

实际发生的是：CON.8 这个编号被用来测 CHECK 9（`"CON.8 CHECK 9: 协议-脚本结构对齐"`），CON.9 也是测 CHECK 9 的 md5 子项。**CHECK 8（v0.6 关键词存在性）在 `consistency.bats` 里依然完全没有测试用例。**

当前 `consistency.bats` 的映射：

| 测试编号 | 对应 CHECK |
|---------|-----------|
| CON.1 | CHECK 1 ✓ |
| CON.2 | CHECK 2 ✓ |
| CON.3 | CHECK 3 ✓ |
| CON.4 | CHECK 4 ✓ |
| CON.5 | CHECK 5 ✓ |
| CON.6 | CHECK 6 ✓ |
| CON.7 | CHECK 7 ✓ |
| CON.8 | **CHECK 9**（不是 CHECK 8）|
| CON.9 | **CHECK 9**（md5 子项）|
| — | **CHECK 8 无测试** |

### 建议

加 CON.10（或把现有 CON.8/CON.9 改编号为 CON.9/CON.10，空出 CON.8 给 CHECK 8）：

```bash
@test "CON.8 CHECK 8: v0.6 关键词存在性" {
    [[ "$CONSISTENCY_OUTPUT" == *"PASS  CHECK 8"* ]]
}
```

---

## 五、🟡 测试计数 4 处互相矛盾

| 位置 | 数字 | 实测 |
|------|------|------|
| `bats` 实际跑 | **162** | 真实值 |
| `count-tests.sh` | 156 | 不含 sanity.bats 的 6 个（156+6=162，逻辑一致但口径不同）|
| `agate/tests/README.md` 覆盖度表 | **154** | 未更新（v0.8 新增了 8 个用例，154+8=162）|
| `AGENTS.md` 版本发布节 | **161** | 未更新（应为 162 或改用 count-tests.sh 自动统计）|

README 的 154 和 AGENTS.md 的 161 都是旧数字——**这正是这个项目自己一直在抓的"计数声明漂移"问题，这次又发生在自己身上。** `count-tests.sh` 的存在就是为了解决这个问题，建议 README 和 AGENTS.md 都引用 `count-tests.sh` 的输出（或改成"以 count-tests.sh 为准"），不要手写死数字。

---

## 六、✅ 计划→实现的偏差追踪

计划锚点表里 P6 provenance 的关键词写的是 `PASS` / `Evidence`，实现里改成了 `EVIDENCE_DIR`——这是一个合理的改进（`EVIDENCE_DIR` 更具体），不是 bug，但说明锚点表在实现过程中做了微调，没有回填到计划文档。由于计划已经归档（`docs/archived/`），这不影响任何活文档，不需要改。

---

## 七、汇总

| # | 问题 | 严重度 | 建议 |
|---|------|--------|------|
| 1 | CHECK 8 无 bats 测试（CON.8 编号被 CHECK 9 占用）| 🟠 | 加 CON.10 或重编号 |
| 2 | 测试计数 4 处不一致（README 154 / AGENTS 161 / count-tests 156 / bats 162）| 🟡 | 统一到 count-tests.sh 口径或写"以脚本为准" |

---

## 八、总评

从"agate 改自己时没有对等机制"这个问题的提出，到这次 `8a55a1d` 的实施落地，经过 2 轮专家评审 + 2 轮复核 + 1 轮终审，最终交付的 CHECK 9 + 角色文件 + 派发模板 + 6 个集成测试，和计划的设计意图高度一致——没有发现设计走样或语义偏差。CHECK 9 的 md5 WARN 输出是真实、有效的（不是凑出来的"看起来在工作"），SG.6 自动验证 11/11 脚本覆盖也是一个好的防退化测试。

两个遗留问题（CHECK 8 测试 + 计数漂移）都是"该做但没做"的补全性工作，不是"做了但做错"的修复性问题，不影响 v0.8 的功能完整性。
