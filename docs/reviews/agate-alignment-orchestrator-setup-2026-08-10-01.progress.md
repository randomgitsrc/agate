start
- diff obtained: README.md/AGENTS.md/orchestrator-template.md/install.sh modified; SETUP.md and assets/templates/project.md are new untracked files
- orchestrator-template.md 全文读毕（115 行）。发现风险点1候选：第13行 warning box 中已出现 {project_root} 占位符引用，而'占位符要靠你自己解析'的说明在第21-24行（之后）——占位符先于说明出现
- SETUP.md 全文读毕（131 行）。project.md 路径引用为 {project_root}/docs/agents/project.md，与 orchestrator-template.md 一致
- project.md 模板全文读毕（38 行）。首行路径 {project_root}/docs/agents/project.md，与 orchestrator-template.md/SETUP.md 三处一致
- 风险点3核实：对本机已装 claude CLI 2.1.226 (claude.exe, node包@anthropic-ai/claude-code) 做了字符串级反汇编分析（非真实会话），定位到自定义 agent frontmatter 解析函数 yfd()。证据：函数手工按字段名逐个读取 r.name/r.description/r.color/r.model/r.background/r.memory/r.isolation/r.effort/r.permissionMode/r.maxTurns/r.tools/r.disallowedTools/r.skills/r.initialPrompt/r.observer*/r.mcpServers，未见对 frontmatter 做 zod .strict() 或"unrecognized_keys"拒绝性校验（那段 unrecognized_keys 字符串出现在别处，与此 agent 加载路径不相关）。结论：mode/permission 这两个字段不在已知字段集合内，会被静默忽略、不会导致解析报错或 agent 加载失败——但它们也不会产生任何效果（无 mode 概念、无 permission 概念），只有 description/color/model 等少数字段被 Claude Code 实际读取。这是本次审查用可复核的方式（非猜测）确认的结果，弥补了原设计"未做等价空跑验证"的缺口，但仍需在报告标注：属于二进制字符串级逆向分析，不是官方文档确认，且新版本可能改变解析逻辑。
- 风险点3追加重大发现（非任务原始提问但审查中定位到）：继续反汇编追踪 eCr()（真正的 markdown frontmatter 解析函数，用 qp(a,s,{normalizeKeys:true}) 解析 YAML frontmatter，未做任何"用文件名回填 name 字段"的兜底逻辑）+ yfd() 中 `let{name:i,description:s}=r;if(!i||typeof i!=="string")return null` + 外层调用处 `if(!m){if(!d.name)return null...}`（无 name 时连警告都不打印，直接跳过该文件，不计入已加载 agent 列表）。结论：Claude Code 官方 subagent frontmatter 规范要求必填 name 字段（与公开文档记忆一致），而当前 agate/orchestrator-template.md frontmatter（第1-9行）只有 description/mode/color/permission，完全没有 name 字段——这意味着 Claude Code 侧会静默地把 .claude/agents/orchestrator.md 整个文件当作"不是一个合法 agent"而跳过加载，SETUP.md 里介绍的注册流程（含 settings.json 里 {"agent":"orchestrator"} 设默认 agent 那步）在 Claude Code 上实际会找不到叫 orchestrator 的 agent。这是一个可复核的具体 MISALIGNED，比任务原本担心的"未知字段是否报错"更严重——不是报错，而是静默失败。
- 风险点2核实：project.md 路径三处（orchestrator-template.md:110, SETUP.md:13/23, project.md模板:3）读取比对，均为 {project_root}/docs/agents/project.md，一致，无旧思路残留
- 反向传播检查：全仓库 grep "orchestrator-template" 命中所有引用文件。发现仓库根 AGENTS.md（非 agate/AGENTS.md，不在本次 diff 范围内）第21行仍写"orchestrator-template.md # 接入新项目时拷贝为 docs/agents/orchestrator.md"——旧"拷贝"措辞残留，未随本次改动同步，属于遗漏。dispatch-protocol.md/state-machine.md/role-system.md/LIMITATIONS.md 均未见旧的"拷贝/复制 orchestrator 该填什么字段"描述，无需同步（各自只是泛引用 orchestrator-template.md 的 mapping 表功能，未描述接入方式细节）。platform-notes.md 未提及 orchestrator 注册细节，与 SETUP.md 不冲突。
- 测试基线：count-tests.sh=597（达标）；bats sanity+unit+regression+integration = 603/603 全绿，0 failed；check-protocol-consistency.py 全部 CHECK 1-9 PASS，0 ERROR（复跑确认，未被后续改动破坏）
- A6 锚点表核查：check-protocol-consistency.py PROTOCOL_FILES 集合（第45-56行）仍显式含 agate/orchestrator-template.md，但新文件 agate/SETUP.md 未加入 PROTOCOL_FILES/PROTOCOL_DIRS——is_protocol_file("agate/SETUP.md") 返回 False。影响：CHECK3（硬编码行号引用检查，仅对 protocol file 生效）不覆盖 SETUP.md；CHECK2（死链检查）仍覆盖（因为非 narrative 文件默认从严，非"仅 protocol file"）。CHECK 9 锚点表（gate 脚本对齐）与本次改动无关，无需新增锚点——本次是纯 onboarding 文档改动，不触及任何 check-*.sh 逻辑。project.md 模板落在 agate/assets/ 下，被 PROTOCOL_DIRS 覆盖，不受此问题影响。
- 反向传播追加发现：README.md:109（卸载一节，未在本次 diff 中）仍写"你的项目里的 docs/agents/orchestrator.md 等文件不会被删"——是旧"拷贝到 docs/agents/orchestrator.md"约定的残留描述，与本次改动后的新约定（.claude/agents/orchestrator.md 或 .opencode/agents/orchestrator.md 符号链接 + 可选 docs/agents/project.md）不一致，未同步。连同 AGENTS.md:21 共两处旧路径残留。
- install.sh 核查：第48行 "$LINK_NAME/SETUP.md" 在 $LINK_NAME 默认 ~/.agate（软链接指向 agate/ 子目录）时正确解析为 agate/SETUP.md，路径准确，ALIGNED
- SETUP.md 第44行 "claude agents --json" 验证命令核查：实测该命令实际列出的是后台/交互会话列表（Manage background agents），不是 .claude/agents/ 目录下的自定义 agent 定义，与"验证 orchestrator 注册"的意图不符，命令本身能跑通但不能验证期望的东西——SETUP.md 注释里已经承认"具体 agent 解析暂无独立校验命令"，但命令选择本身有误导性（看起来像在验证 agent 而实际验证的是别的东西）
- role-system.md"方法 B"引用核查（SETUP.md:11 提到）：role-system.md:139-141 确有"方法 B（退路）"小节，描述吻合，ALIGNED
- A7 ADR 检查：查 agate/adr.md 中是否有与本次改动（orchestrator 符号链接接入模式）相关的 ADR
- A7 ADR 结论：adr.md 现有 ADR-001~007 均未覆盖"orchestrator-template.md 符号链接单文件接入 + project.md 分离项目信息"这一架构决策（这是本次改动的核心设计转变：从逐项目拷贝改字段，改为全项目内容完全一致的符号链接）。属于未记录的架构决策，建议补 ADR-008。判 NEEDS_HUMAN_REVIEW（A7 规则不存在 MISALIGNED）
- 全部核查项完成，开始写产出报告
- 报告文件已写入 docs/reviews/agate-alignment-orchestrator-setup-2026-08-10.md，审查完成
