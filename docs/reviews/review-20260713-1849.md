---
review_target: v0.13.0-protocol-fixes-20260713.md（v5：P1-1 撤下、矛盾1/2 → P2-6/P2-7）
reviewer: 独立第三方（非方案作者，非 self-review）
date: 2026-07-13
prev: review-v0.13.0-plan-r5-20260713.md
verdict: approve-with-minor-revisions（第 5 轮三条全部正确采纳；剩 1 处 should-fix + 1 处精度 + 2 处文档 artifact，无新 blocking）
method: 逐条核对采纳结果 + P4 gate .yaml 排除范围 + WORKFLOW:132 重叠 + 平台无关表残留引用
---

# v0.13.0 方案 v5 第 6 轮评审

方案已基本收敛。第 5 轮三条全部正确采纳，无新 blocking。剩几处小的自洽清理。

## 1. 第 5 轮采纳正确（给信任）

- **P1-1 撤下**：清单里划除，有效内核"域触发硬要求评审"并入 **P3-1**（line 93），并保留了三段证据链（C8 域触发 / exit 2 不阻断 / 与 P1-3 矛盾）。✅ 采纳到位。
- **矛盾 1 → P2-6**：改写 P1 卡片:72"对称"措辞为"P1 通用、P2/P4 域触发、不对称"。✅
- **矛盾 2 → P2-7**：删 WORKFLOW 文档任务范畴以对齐软件工程定位。✅（但删得需更精确，见 §3）

P1-2..P1-8 维持前几轮已验证的正确结论（P1-2 已定义 `$P1_FILE`+插入点明确、P1-5 已去引号、P1-7 依 state-machine:190、P1-8 三处协同准确）。§0.5 滤网回筛也已把撤下的 P1-1 移入"已删除"。

## 2. Should-fix — 平台无关表残留已撤下的 P1-1（map 未跟上 territory）

平台无关表 P4 行（line 346）仍写：
> P4-implementation | ✅ 成立 | P1-7 修后脚本与协议一致；**P1-1 修后 P4-review.md 无条件必需**

但 P1-1 本轮已撤下。这句"P1-1 修后 P4-review.md 无条件必需"是撤下前的残留，与 line 68/121 的撤下**自相矛盾**。方案自己强调"先修地图再改领土"，这正是地图没跟上的一处。

**修法**：P4 行改为"P1-7 修后脚本与协议一致（P4-review 必需性归 P3-1 域触发）"，删掉 P1-1 那半句。列入 P0 清理。

## 3. 精度 — P2-7"删文档/配置类任务"把合法的**配置任务**一起删了；且 P4 gate 的 .yaml 排除是更深的根

这条我按前车之鉴（上次我在"纯文档任务"上 over-reach）**克制地**提，供你判断，不断言是 showstopper：

P2-7 要删的 WORKFLOW:132 原文是"① **纯文档/配置类任务**——没有可测试的行为（如更新 README、调整配置文件）"。它把两类捆在一起：
- **纯文档任务**：按 README 定位 + 你的澄清，出 agate 范围 → 删对。
- **配置类任务**：改 CI/k8s/compose/app 配置——**这是正经软件工程工作，不是文档**。整条删掉会把合法的配置任务也扫出去。

而且更深一层：即便保留配置任务，**P4 gate（check-gate.sh:107）排除的是全部 `.yaml$`**（不只 `.state.yaml`）。所以一个纯配置交付的任务（如"迁移 CI 到 GHA"、改 k8s manifest，暂存只有 .yaml）走到 P4 会被当"非实现"`exit 1` 拦——这跟"纯文档任务过不了 P4"是同一个根（P4 gate 的交付物类型假设过窄），只是换成 .yaml。

**修法建议**（把 矛盾 2 修彻底，而非只删范畴）：
- P2-7 只删"**纯文档**任务"表述，**保留配置任务**（它是软件工作）；
- 配套把 P4 gate 的排除从 `\.(md|yaml)$|^\.state` 收窄为**只排流程产物**（`P[0-8]-.*\.md$` 与 `\.state\.yaml$`），这样配置类 .yaml 交付能过 P4，纯文档 .md 若确出范围则由 P2-7 的范畴删除来挡（而非靠 gate 硬拦）。

若你判定"配置类任务在 agate 目标里也少见/都走微任务直做"，则维持整条删除亦可——但请在 P2-7 里**显式写明"配置类任务归微任务直做"**，别留"配置任务算什么"的空白。这条我不锁死，交你定。

## 4. Minor（文档 artifact）

- **line 135 孤立的 ` ``` `**：撤下的 ~~P1-1~~ 段落（blockquote）末尾留了一个没有配对开头的代码围栏，会让后面 P1-2 的渲染错位。删掉这行。
- **头部"3 轮专家评审"（line 6/11）已过时**：现在到第 5 轮，且冲突裁决区从"第三轮"直接跳到"第五轮"，缺"第四轮"（P4-review/"若有触发"那轮——虽被第 5 轮的"撤下"取代，但演变链断了一环）。改为"5 轮"并补一句第四轮如何被第五轮取代，或直接标注"第四轮结论已并入第五轮撤下决定"。

## 5. 判定

**approve-with-minor-revisions。** 第 5 轮三条全部正确采纳，P1 代码清单（P1-2..P1-8）经多轮实核稳定，P1-1 正确撤下、复杂项诚实归 P3-1，两处协议矛盾正确路由到 P2-6/P2-7。剩下的是自洽清理：

1. **should-fix**：平台无关表 P4 行删掉残留的"P1-1 修后…无条件必需"（§2）
2. **精度（交你定）**：P2-7 区分纯文档（删）与配置任务（软件工作，保留）；若保留配置任务则配套收窄 P4 gate 的 .yaml 排除为仅流程产物（§3）
3. **minor**：删 line 135 孤立代码围栏；头部"3 轮"→"5 轮"并补第四轮衔接（§4）

修完 §2 与 §4 即可进实施（§3 视你对配置任务是否在范围内的判断决定改法）。这版没有代码级 blocking——是我评这个方案六轮以来第一次能给"可实施"的判定。

**提醒**：本轮依据是 P4 gate 的 .yaml 排除实读 + WORKFLOW:132 原文 + 平台表逐格核对。§3 我特意标注"克制、交你定"——因为它与我此前 over-reach 的"纯文档任务"同族，我不想再把一个窄边角说成硬伤；但配置类 .yaml 交付在真实软件项目里（CI/k8s）相当常见，值得你在删 P2-7 范畴时顺手想清楚，而不是留白。
