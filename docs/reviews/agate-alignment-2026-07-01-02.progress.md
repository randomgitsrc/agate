- 意图: 把 self-gate 从'审 diff 对不对'升级为'审 diff + 反向传播是否覆盖到应被影响的文件'。新增 A3b（反向传播）+ A5 文档传播 + 反向传播常见路径表 + 派发模板加意图/反向传播步骤。
- SELF-GATE.md: 重写派发模板，加意图分析/反向传播/第三步实际审查范围；全量审查模板加'意图无关但反向传播'步骤；分批派发强调协议+脚本配对
- protocol-alignment-review.md: A3 拆 A3a+A3b 加反向传播定义；A5 加文档传播；新增'反向传播常见路径'表（7 条映射）
- orchestrator-template.md:82 改为「重试超限：P3/P5/P6/P7/P8 ≥2 次、P1/P2/P4 ≥3 次」——和 state-machine.md:429-438 的 MAX_RETRY 表一致
- check-retrospective.bats: 新增 RT.5 (P3 retries=2 触发) + RT.6 (P3 retries=1 不触发)；RT.2 仍测 P2=3；共 6 用例
- state-machine.md:427-438 重试上限表确认：P1/P2/P4=3, P3/P5/P6/P7/P8=2，与 orchestrator 描述一致
- dispatch-protocol.md: 未提到具体重试次数，统一用 MAX_RETRY(Pn) 引用（不需要改）
- 反向传播命中1: git-integration.md:168 仍写「重试 ≥3」——本次 commit 同时改了 orchestrator-template.md 类似的描述，但 git-integration.md 没改
- 反向传播分析: WORKFLOW.md:274 「按阶段 2-3 次」是对的泛指，不需改；dispatch-protocol.md 用 MAX_RETRY(Pn) 不需改；loop-orchestration.md:134 同样泛指，不需改
- check-retrospective.sh:14 注释已说明「按阶段差异化 MAX_RETRY，与 check-state-transition.sh 保持同步」；21 行的 max_map 字面值为 P1:3,P2:3,P3:2,P4:3,P5:2,P6:2,P7:2,P8:2，与 state-machine.md 表一致
- CHANGELOG.md [Unreleased]: 当前只有上次 ff05aa5 改动（MAX_RETRY per-phase + 回退跳变恢复 exit 1），没加本次 133c7b4 (self-gate 反向传播) 条目——A5 文档传播不完整
- README.md: 不涉及 self-gate/reverse-prop 主题，无版本 badge 变更需要——不需要改
- agate/AGENTS.md: 不涉及 self-gate 角色描述，无变更
- LIMITATIONS.md 局限 5: 提到了 protocol-alignment-review + CHECK 9 + A1-A6，未提反向传播。本次 A3 拆 A3a+A3b、A5 加文档传播——该局限 5 描述仍准确（描述的是审查机制存在，没要求反向传播是必须的）
- role-system.md: 不涉及 retry/MAX_RETRY 描述，无变更
- platform-notes.md: 不涉及 retry，无变更
- 反向传播命中2: tests/README.md:38 写「check-retrospective.bats | 4」，但实际 @test=6（RT.5/RT.6 是本次 commit 新加）
- 反向传播命中3: tests/README.md:36 写「check-state-transition.bats | 8」，但 count-tests.sh 输出 15——但这不是本次 commit 引起的（git log 中 check-state-transition.bats 不在 133c7b4 里）。本次 commit 范围不修，但点出文档漂移
- scripts/README.md: 不涉及 self-gate/reverse-prop 主题——脚本清单没改（check-retrospective.sh 行为没变，只是被更细的 MAX_RETRY 测试覆盖）——不需要改
- .github/workflows/protocol-tests.yml: 不涉及 self-gate 行为，只跑 bats/shellcheck/consistency——不需要改
- check-state-transition.sh:21 / check-retrospective.sh:14 字面值 P1:3,P2:3,P3:2,P4:3,P5:2,P6:2,P7:2,P8:2 一致，state-machine.md:429-438 表一致——A1 ALIGNED
- 反向传播命中4: protocol-alignment-review.md:68-75 总结表里 A3/A5 仍写「一致性连锁」「下游影响」（旧标签），但审查清单 (line 22-24) 已改为「一致性连锁 + 反向传播」「下游影响 + 文档传播」——同一文件内部不一致
- dispatch-prompt.md: 不涉及 self-gate/reverse-prop 主题（这是给项目侧的派发模板，不是 self-gate 派发）——不需要改
- A6 锚点表覆盖: protocol-alignment-review.md 中的 A1-A6 标签仍写「一致性连锁」「下游影响」，check-protocol-consistency.py 的 SCRIPT_ALIGNMENT_ANCHORS 表无 A1-A6 锚点（CHECK 9 只看脚本关键词），不影响结构兜底——但 A6 语义层要求 review subagent 报告含 A1-A6 全六项，本文件本身的输出模板仍是旧标签
- 反向传播命中5 (可论证为 NEEDS_HUMAN_REVIEW): 集成测试 protocol-alignment-review.bats 只检查 A1-A6 是否 grep 命中，不验证 A3 拆 A3a+A3b 的语义是否被审查 subagent 实际执行——本次新增 A3b 反向传播概念，但 SG.2 测试仍是 grep A3 命中即可通过。A6 锚点表覆盖仅检查文件存在性，不验证反向传播机制运行
- SG.4/5 测试 SELF-GATE.md 的字符串存在性，本次扩写后能通过；tests/integration 6 用例都没改——A4 测试覆盖仅保证子串命中，不保证 subagent 实际产出反向传播段落（机制运行时验证靠 manual）
- protocol-alignment-review.bats: SG.1-SG.6 测试套件不需要改（核心断言 A1-A6 存在 + SELF-GATE.md 含派发模板 + CHECK 9 锚点表覆盖依然通过）
