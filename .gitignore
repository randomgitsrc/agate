# gate result（每 commit 由 pre-commit hook 重新生成）
.gate-result.json

# gate 历史（运行时追加，不是仓库内容）
.gate-history.jsonl

# agate 运行时状态（项目级，不是协议仓库内容）
.state.yaml

# Python
__pycache__/
*.pyc

# 编辑器临时文件
*.swp
*.swo
*.bak
*~
.DS_Store
