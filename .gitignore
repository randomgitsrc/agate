# gate result（每 commit 由 pre-commit hook 重新生成）
.gate-result.json

# gate 历史（运行时追加，不是仓库内容）
.gate-history.jsonl

# OpenCode 项目级接入层（软链 → docs/converse/agents，本地环境配置不入库）
.opencode/

# Claude Code 项目级接入层（软链 → docs/converse/agents，本地环境配置不入库）
.claude/

# Python
__pycache__/
*.pyc

# 编辑器临时文件
*.swp
*.swo
*.bak
*~
.DS_Store
.worktrees/
