# P2/P6 不可裁剪 + P7 隐式耦合声明加强 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** P2 和 P6 改为不可裁剪（无例外口），P7 裁剪时"无隐式耦合"声明须列出检查点

**Architecture:** 修改 check-pruning.sh 裁剪逻辑 + 更新 8 个协议文档 + 更新测试 + 更新锚点表

**Tech Stack:** bash, bats, python3

---

## 变更概览

| 变更 | 当前 | 改为 |
|------|------|------|
| P2 裁剪 | 不可裁（例外口：design_trivial / follows_existing_pattern / legacy_p2_pruned）| **不可裁（无例外口）** |
| P2 候选方案 | design_trivial/follows_existing_pattern 时可只写 1 个 | **始终 ≥2**（design_trivial/follows_existing_pattern 语义变为"P2 可简化但不可省略"）|
| P6 裁剪 | 不可裁（例外口：no_behavior_change: true）| **不可裁（无例外口）** |
| P7 裁剪 | implicit_coupling 字段存在则不可裁 | 同上，但声明"无隐式耦合"时须列出检查过的耦合点 |
| design_trivial | P2 裁剪例外口 | **保留字段**，语义变为"P2 可简化（1 个候选方案），不可省略" |
| follows_existing_pattern | P2 裁剪例外口 | **保留字段**，语义变为"P2 可简化（1 个候选方案），不可省略" |
| legacy_p2_pruned | P2 裁剪过渡期标记 | **删除** |
| no_behavior_change | P6 裁剪例外口 | **保留字段**，语义变为"P6 可简化（快速验收），不可省略" |

## 文件影响

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| agate/scripts/check-pruning.sh | 修改 | 检查 2 删除 P2 例外口；检查 3 删除 P6 例外口 |
| agate/tests/unit/check-pruning.bats | 修改 | 删 4 个 P2 例外口测试 + 2 个 P6 例外口测试，加 2 个硬拦截测试 |
| agate/tests/unit/check-gate.bats | 修改 | G2.5 design_trivial 裁剪测试需更新 |
| agate/state-machine.md | 修改 | 裁剪规则 P2/P6 描述 |
| agate/WORKFLOW.md | 修改 | P2/P6 阶段总览行 |
| agate/dispatch-protocol.md | 修改 | 门槛表 + Pre-commit 检查全景 |
| agate/orchestrator-template.md | 修改 | P2/P6 裁剪提醒 |
| agate/phase-cards/P2-design.md | 修改 | 裁剪跳阶说明 + 候选方案例外 |
| agate/phase-cards/P6-acceptance.md | 修改 | 裁剪跳阶说明 |
| agate/assets/execution-roles/architect.md | 修改 | design_trivial/follows_existing_pattern 语义 |
| agate/assets/templates/task-files.md | 修改 | 候选方案例外说明 |
| agate/scripts/check-protocol-consistency.py | 修改 | 锚点表 P2/P6 裁剪条件描述 |
| CHANGELOG.md | 修改 | 标注变更 |

---

### Task 1: check-pruning.sh — P2 不可裁（删除例外口）

**Files:**
- Modify: `agate/scripts/check-pruning.sh:46-60`

- [ ] **Step 1: 写失败测试**

在 `agate/tests/unit/check-pruning.bats` 中，将 P2.3a/P2.3b/P2.3c/P2.3d 四个测试改为期望 exit 1：

```bash
@test "P2.3a check-pruning.sh 裁剪 P2 + legacy_p2_pruned: true 期望 exit 1 (P2 不可裁)" {
    local dir
    dir=$(create_task_dir P0 P1 P3 P4 P5 P6 P7 P8)
    add_p1_field "$dir" "legacy_p2_pruned" "true"
    add_pruning_excuse "$dir" P2 "v0.5 任务" "低"
    run bash "$AGATE_SCRIPTS/check-pruning.sh" "$dir"
    [ "$status" -eq 1 ]
    [[ "$output" == *"P2 不可裁剪"* ]]
}

@test "P2.3b check-pruning.sh 裁剪 P2 + design_trivial: true 期望 exit 1 (P2 不可裁)" {
    local dir
    dir=$(create_task_dir P0 P1 P3 P4 P5 P6 P7 P8)
    add_p1_field "$dir" "design_trivial" "true"
    add_pruning_excuse "$dir" P2 "文案修改" "低"
    run bash "$AGATE_SCRIPTS/check-pruning.sh" "$dir"
    [ "$status" -eq 1 ]
    [[ "$output" == *"P2 不可裁剪"* ]]
}

@test "P2.3c check-pruning.sh 裁剪 P2 + follows_existing_pattern 期望 exit 1 (P2 不可裁)" {
    local dir
    dir=$(create_task_dir P0 P1 P3 P4 P5 P6 P7 P8)
    add_p1_field "$dir" "follows_existing_pattern" "[src/foo.py]"
    add_pruning_excuse "$dir" P2 "照搬" "低"
    run bash "$AGATE_SCRIPTS/check-pruning.sh" "$dir"
    [ "$status" -eq 1 ]
    [[ "$output" == *"P2 不可裁剪"* ]]
}
```

删除 P2.3d（空参照测试，已无意义——P2 不可裁后空参照不再是独立 case）。

同时更新 P2.9a 测试——该测试同时用了 legacy_p2_pruned + override，P2 不可裁后 legacy_p2_pruned 不再放行，测试需改为期望 exit 1：

```bash
@test "P2.9a check-pruning.sh 裁剪 P2 + override + 产出文件 期望 exit 1 (P2 不可裁)" {
    local dir
    dir=$(create_task_dir P0 P1 P3 P4 P5 P6 P7 P8)
    echo "actual design" > "$dir/P2-design.md"
    add_p1_field "$dir" "override" "P2 retained manually"
    add_p1_field "$dir" "legacy_p2_pruned" "true"
    add_pruning_excuse "$dir" P2 "理由" "低"
    run bash "$AGATE_SCRIPTS/check-pruning.sh" "$dir"
    [ "$status" -eq 1 ]
    [[ "$output" == *"P2 不可裁剪"* ]]
}
```

- [ ] **Step 2: 跑测试确认红**

```bash
bats agate/tests/unit/check-pruning.bats --filter "P2.3|P2.9a"
```

预期：P2.3a/P2.3b/P2.3c 期望 exit 1 但实际 exit 0（例外口仍放行）

- [ ] **Step 3: 修改 check-pruning.sh 检查 2**

将 L46-60 替换为：

```bash
# 检查 2：P2 不可裁剪（无例外口）
if ! echo "$PHASES_DECLARED" | grep -qw 'P2'; then
    ERRORS="${ERRORS}P2 不可裁剪——方案设计是必经阶段，P1 analyst 做需求分析不做方案设计，无法预知 P2 architect 会发现哪些隐含问题。design_trivial / follows_existing_pattern 可简化 P2（1 个候选方案），不可省略 P2\n"
fi
```

- [ ] **注意**：删除 legacy_p2_pruned / design_trivial / follows_existing_pattern 三个例外口分支，只保留硬拦截。

- [ ] **Step 4: 跑测试确认绿**

```bash
bats agate/tests/unit/check-pruning.bats --filter "P2.3|P2.9a"
```

- [ ] **Step 5: 更新 check-gate.bats G2.5**

G2.5 测试 "P2 无 P2 文件（design_trivial 裁剪）期望 exit 2"——P2 不可裁后，design_trivial 不再是裁剪理由，该测试应改为"P2 无 P2 文件期望 exit 1"：

```bash
@test "G2.5 check-gate.sh P2 无 P2 文件 期望 exit 1" {
    local dir
    dir=$(create_task_dir)
    run bash "$AGATE_SCRIPTS/check-gate.sh" P2 "$dir"
    [ "$status" -eq 1 ]
    [[ "$output" == *"P2-design.md"* ]]
}
```

- [ ] **Step 6: 跑 check-gate.bats 确认**

```bash
bats agate/tests/unit/check-gate.bats --filter "G2.5"
```

- [ ] **Step 7: Commit**

```bash
git add agate/scripts/check-pruning.sh agate/tests/unit/check-pruning.bats agate/tests/unit/check-gate.bats
git commit -m "P2不可裁: 删除design_trivial/follows_existing_pattern/legacy_p2_pruned例外口"
```

---

### Task 2: check-pruning.sh — P6 不可裁（删除 no_behavior_change 例外口）

**Files:**
- Modify: `agate/scripts/check-pruning.sh:62-67`

- [ ] **Step 1: 写失败测试**

将 P2.4a 和 P2.12a 改为期望 exit 1：

```bash
@test "P2.4a check-pruning.sh 裁剪 P6 + no_behavior_change: true 期望 exit 1 (P6 不可裁)" {
    local dir
    dir=$(create_task_dir P0 P1 P2 P3 P4 P5 P7 P8)
    add_p1_field "$dir" "no_behavior_change" "true"
    add_pruning_excuse "$dir" P6 "无行为变更" "低"
    run bash "$AGATE_SCRIPTS/check-pruning.sh" "$dir"
    [ "$status" -eq 1 ]
    [[ "$output" == *"P6 不可裁剪"* ]]
}
```

删除 P2.12a（P6 裁剪 + no_behavior_change + 跳过风险 → exit 0），改为 exit 1 测试。

P2.12（P6 裁剪无跳过风险）保留但改为期望 exit 1 且输出含 "P6 不可裁剪"（因为 P6 不可裁后，有无跳过风险都 exit 1，但错误信息不同——先报 P6 不可裁）。

- [ ] **Step 2: 跑测试确认红**

```bash
bats agate/tests/unit/check-pruning.bats --filter "P2.4|P2.12"
```

- [ ] **Step 3: 修改 check-pruning.sh 检查 3**

将 L62-67 替换为：

```bash
# 检查 3：P6 不可裁剪（无例外口）
if ! echo "$PHASES_DECLARED" | grep -qw 'P6'; then
    ERRORS="${ERRORS}P6 不可裁剪——验收是质量最后防线。no_behavior_change 可简化 P6（快速验收），不可省略 P6\n"
fi
```

- [ ] **Step 4: 跑测试确认绿**

```bash
bats agate/tests/unit/check-pruning.bats --filter "P2.4|P2.12"
```

- [ ] **Step 5: Commit**

```bash
git add agate/scripts/check-pruning.sh agate/tests/unit/check-pruning.bats
git commit -m "P6不可裁: 删除no_behavior_change例外口"
```

---

### Task 3: 协议文档同步 — P2/P6 不可裁

**Files:**
- Modify: `agate/state-machine.md:164,166`
- Modify: `agate/WORKFLOW.md:194`
- Modify: `agate/dispatch-protocol.md:649`
- Modify: `agate/orchestrator-template.md:92-93`
- Modify: `agate/phase-cards/P2-design.md:4,40,47-48`
- Modify: `agate/phase-cards/P6-acceptance.md:4`
- Modify: `agate/assets/execution-roles/architect.md:24`
- Modify: `agate/assets/templates/task-files.md:186`

- [ ] **Step 1: state-machine.md**

L164 替换：
```
  - 裁剪 P2：不可裁（方案设计是必经阶段。P1 analyst 做需求分析不做方案设计，无法预知 P2 architect 会发现哪些隐含问题。design_trivial / follows_existing_pattern 可简化 P2（1 个候选方案），不可省略 P2）
```

L166 替换：
```
  - 裁剪 P6：不可裁（验收是质量最后防线。no_behavior_change 可简化 P6（快速验收），不可省略 P6）
```

- [ ] **Step 2: WORKFLOW.md**

L194 P2 阶段总览行，将 `v0.6 不可裁（例外口：design_trivial / follows_existing_pattern）` 改为 `不可裁（design_trivial / follows_existing_pattern 可简化，不可省略）`。

将 `v0.6 强制 ≥2 个候选方案（design_trivial/follows_existing_pattern 时跳过）` 改为 `强制 ≥2 个候选方案（design_trivial/follows_existing_pattern 时可只写 1 个）`。

- [ ] **Step 3: dispatch-protocol.md**

L649 将 `裁剪条件 + override 校验（v0.6：P2 不可裁，例外口 design_trivial / follows_existing_pattern）` 改为 `裁剪条件 + override 校验（P2/P6 不可裁；design_trivial / follows_existing_pattern / no_behavior_change 可简化但不可省略）`。

- [ ] **Step 4: orchestrator-template.md**

L92 将 `永远不要在没有 no_behavior_change: true 时裁剪 P6（不验证 P6 意味着没验收）` 改为 `P6 不可裁剪——验收是质量最后防线。no_behavior_change 可简化 P6（快速验收），不可省略`。

L93 将 `永远不要在没有 design_trivial: true 或 follows_existing_pattern: [参照文件] 或 legacy_p2_pruned: true 时裁剪 P2（v0.6：方案设计是必经阶段，P1 看不到 P2 会发现什么）` 改为 `P2 不可裁剪——方案设计是必经阶段。design_trivial / follows_existing_pattern 可简化 P2（1 个候选方案），不可省略`。

- [ ] **Step 5: phase-cards/P2-design.md**

L4 将裁剪跳阶说明改为：`P2 不可裁剪——design_trivial / follows_existing_pattern 可简化（1 个候选方案），不可省略。`

L40 将 `候选方案 ≥2 + 权衡 + 选择理由（design_trivial / follows_existing_pattern 时例外，见下方）` 改为 `候选方案 ≥2 + 权衡 + 选择理由（design_trivial / follows_existing_pattern 时可只写 1 个，见下方）`

L47-48 将：
```
- `design_trivial: true` → 可只写 1 个方案
- `follows_existing_pattern: [src/foo.py]` → 可只写 1 个方案（参照已有模式）
```
改为：
```
- `design_trivial: true` → 可只写 1 个候选方案（P2 仍不可省略）
- `follows_existing_pattern: [src/foo.py]` → 可只写 1 个候选方案，参照已有模式（P2 仍不可省略）
```

- [ ] **Step 6: phase-cards/P6-acceptance.md**

L4 将裁剪跳阶说明改为：`P6 不可裁剪——no_behavior_change 可简化（快速验收），不可省略。`

- [ ] **Step 7: architect.md**

L24 将 `例外口：design_trivial: true 或 follows_existing_pattern: [参照文件] 时可只写 1 个` 改为 `design_trivial: true 或 follows_existing_pattern: [参照文件] 时可只写 1 个候选方案（P2 仍不可省略）`。

- [ ] **Step 8: task-files.md**

L186 将 `# 例外口：design_trivial: true 或 follows_existing_pattern: [参照文件] 时可只写 1 个` 改为 `# design_trivial: true 或 follows_existing_pattern: [参照文件] 时可只写 1 个候选方案（P2 仍不可省略）`。

- [ ] **Step 9: 全文搜索确认无残留**

```bash
grep -rn 'legacy_p2_pruned' agate/ --include='*.md' --include='*.sh'
grep -rn '例外口' agate/ --include='*.md' --include='*.sh'
grep -rn 'P2.*裁剪.*design_trivial.*放行\|P2.*裁剪.*follows.*放行' agate/ --include='*.md'
```

预期：legacy_p2_pruned 0 结果；"例外口" 0 结果（或仅出现在非 P2 上下文）。

- [ ] **Step 10: Commit**

```bash
git add agate/state-machine.md agate/WORKFLOW.md agate/dispatch-protocol.md agate/orchestrator-template.md agate/phase-cards/P2-design.md agate/phase-cards/P6-acceptance.md agate/assets/execution-roles/architect.md agate/assets/templates/task-files.md
git commit -m "文档同步: P2/P6不可裁, design_trivial/follows_existing_pattern/no_behavior_change语义改为可简化不可省略"
```

---

### Task 4: 锚点表 + consistency 更新

**Files:**
- Modify: `agate/scripts/check-protocol-consistency.py:445-448,455-458`

- [ ] **Step 1: 更新锚点表 P2 裁剪条件描述**

L445-448 将：
```python
    {
        "desc": "裁剪 P2 条件（design_trivial / follows_existing_pattern / legacy_p2_pruned）",
        "script": "agate/scripts/check-pruning.sh",
        "keywords": ["design_trivial", "follows_existing_pattern", "legacy_p2_pruned"],
    },
```
改为：
```python
    {
        "desc": "P2 不可裁剪（design_trivial / follows_existing_pattern 可简化不可省略）",
        "script": "agate/scripts/check-pruning.sh",
        "keywords": ["P2 不可裁剪"],
    },
```

- [ ] **Step 2: 更新锚点表 P6 裁剪条件描述**

L455-458 将：
```python
    {
        "desc": "裁剪 P6 条件（no_behavior_change）",
        "script": "agate/scripts/check-pruning.sh",
        "keywords": ["no_behavior_change"],
    },
```
改为：
```python
    {
        "desc": "P6 不可裁剪（no_behavior_change 可简化不可省略）",
        "script": "agate/scripts/check-pruning.sh",
        "keywords": ["P6 不可裁剪"],
    },
```

- [ ] **Step 3: 更新 CHECK 8 关键词**

L415 将 `("design_trivial", "agate/scripts/check-pruning.sh", "裁剪检查脚本")` 改为 `("P2 不可裁剪", "agate/scripts/check-pruning.sh", "裁剪检查脚本")`。

L416 将 `("design_trivial", "agate/state-machine.md", "状态机文档")` 改为 `("P2 不可裁剪", "agate/state-machine.md", "状态机文档")`。

- [ ] **Step 4: 跑 consistency 确认 0 ERROR**

```bash
python3 agate/scripts/check-protocol-consistency.py
```

- [ ] **Step 5: Commit**

```bash
git add agate/scripts/check-protocol-consistency.py
git commit -m "锚点表: P2/P6不可裁剪关键词更新"
```

---

### Task 5: P7 隐式耦合声明加强

**Files:**
- Modify: `agate/scripts/check-pruning.sh:88-93`
- Modify: `agate/state-machine.md:167`
- Add test in: `agate/tests/unit/check-pruning.bats`

- [ ] **Step 1: 写失败测试**

在 check-pruning.bats 检查 5/6 区域后追加：

```bash
@test "P2.6d check-pruning.sh 裁剪 P7 无 implicit_coupling 但缺 coupling_checklist 期望 exit 1" {
    local dir
    dir=$(create_task_dir P0 P1 P2 P3 P5 P6 P8)
    add_pruning_excuse "$dir" P7 "小改动" "低"
    # 不写 implicit_coupling 也不写 coupling_checklist
    run bash "$AGATE_SCRIPTS/check-pruning.sh" "$dir"
    [ "$status" -eq 1 ]
    [[ "$output" == *"coupling_checklist"* ]]
}

@test "P2.6e check-pruning.sh 裁剪 P7 + coupling_checklist 放行" {
    local dir
    dir=$(create_task_dir P0 P1 P2 P3 P5 P6 P8)
    add_p1_field "$dir" "coupling_checklist" "[api-schema: checked, data-model: checked]"
    add_pruning_excuse "$dir" P7 "小改动" "低"
    local repo
    repo=$(git_init)
    for i in 1 2 3; do
        echo "file $i" > "$repo/src_$i.py"
    done
    git_commit "$repo" "init"
    cp -r "$dir" "$repo/task"
    git -C "$repo" add src_*.py
    run bash -c "cd '$repo' && bash '$AGATE_SCRIPTS/check-pruning.sh' 'task'"
    [ "$status" -eq 0 ]
}
```

- [ ] **Step 2: 跑测试确认红**

```bash
bats agate/tests/unit/check-pruning.bats --filter "P2.6d"
```

- [ ] **Step 3: 修改 check-pruning.sh**

在检查 5 的 implicit_coupling 检查后追加 coupling_checklist 检查：

```bash
    # R4(c)：裁剪 P7 时，若未声明 implicit_coupling，须有 coupling_checklist
    # 列出检查过的耦合点（防止无脑写"无隐式耦合"）
    if ! grep -qE '^implicit_coupling:' "$P1_FILE" 2>/dev/null; then
        if ! grep -qE '^coupling_checklist:\s*\[' "$P1_FILE" 2>/dev/null; then
            ERRORS="${ERRORS}裁剪 P7 需 coupling_checklist: [检查过的耦合点]（如 api-schema: checked, data-model: checked）\n"
        fi
    fi
```

- [ ] **Step 4: 跑测试确认绿**

```bash
bats agate/tests/unit/check-pruning.bats --filter "P2.6"
```

- [ ] **Step 5: 更新 state-machine.md**

L167 将：
```
  - 裁剪 P7：需源码文件数 ≤ 5 AND 无 implicit_coupling 声明（隐式耦合维度，self-declaration。如共享 CSS class、API schema、数据模型、配置项等）
```
改为：
```
  - 裁剪 P7：需源码文件数 ≤ 5 AND 无 implicit_coupling 声明 AND 有 coupling_checklist（列出检查过的耦合点，如 api-schema: checked, data-model: checked。防止无脑写"无隐式耦合"）
```

- [ ] **Step 6: Commit**

```bash
git add agate/scripts/check-pruning.sh agate/tests/unit/check-pruning.bats agate/state-machine.md
git commit -m "P7裁剪: coupling_checklist强制列出检查过的耦合点"
```

---

### Task 6: CHANGELOG + 全量验证

**Files:**
- Modify: `CHANGELOG.md`

- [ ] **Step 1: 更新 CHANGELOG [Unreleased]**

追加：
```
### 变更
- **P2 不可裁剪**：删除 design_trivial / follows_existing_pattern / legacy_p2_pruned 例外口。design_trivial / follows_existing_pattern 语义改为"可简化 P2（1 个候选方案），不可省略 P2"
- **P6 不可裁剪**：删除 no_behavior_change 例外口。no_behavior_change 语义改为"可简化 P6（快速验收），不可省略 P6"
- **P7 裁剪加强**：声明"无隐式耦合"时须有 coupling_checklist 列出检查过的耦合点
```

- [ ] **Step 2: 全量 bats**

```bash
bats agate/tests/sanity.bats agate/tests/unit/ agate/tests/regression/ agate/tests/integration/
```

预期：全部通过

- [ ] **Step 3: consistency**

```bash
python3 agate/scripts/check-protocol-consistency.py
```

预期：0 ERROR

- [ ] **Step 4: shellcheck**

```bash
shellcheck agate/scripts/check-pruning.sh
```

预期：0 error

- [ ] **Step 5: 残留搜索**

```bash
grep -rn 'legacy_p2_pruned' agate/ --include='*.md' --include='*.sh'
grep -rn 'P2.*例外口' agate/ --include='*.md'
grep -rn 'P6.*no_behavior_change.*放行\|P6.*no_behavior_change.*裁' agate/ --include='*.md' --include='*.sh'
```

预期：0 结果

- [ ] **Step 6: Commit**

```bash
git add CHANGELOG.md
git commit -m "CHANGELOG: P2/P6不可裁 + P7 coupling_checklist"
```

---

### Task 7: Self-gate 评审

- [ ] **Step 1: 派发 protocol-alignment-review subagent**

变更意图：P2/P6 从"有例外口的不可裁"升级为"无例外口的不可裁"，P7 裁剪增加 coupling_checklist 声明要求。

- [ ] **Step 2: 读审查报告，MISALIGNED 必修**

- [ ] **Step 3: 全量 bats 重跑确认**

- [ ] **Step 4: 最终 commit + push**
